# Single source of truth for Lambda runtime dependencies is src/requirements.txt.
# SAM build uses CodeUri=src/ and reads src/requirements.txt directly; this file
# delegates to it so `pip install -r requirements.txt` (local dev) installs the
# same set the Lambda package will receive.
-r src/requirements.txt
