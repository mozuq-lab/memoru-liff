# TASK-0140: フロントエンド型定義・API クライアント

**タスクID**: TASK-0140
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 2 - フロントエンド実装
**信頼性レベル**: 🔵 *設計文書 architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/card-back-ai-assist/requirements.md) - REQ-402
- **設計文書**: [📐 architecture.md](../../design/card-back-ai-assist/architecture.md)

## タスク概要

フロントエンドに AI 補足機能の型定義と API クライアントメソッドを追加する。バックエンドの RefineCardRequest/Response に対応する TypeScript 型と、`POST /cards/refine` を呼び出す refineCard() メソッドを実装する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0141: CardForm 「AI で補足」ボタン実装](TASK-0141.md)

## 完了条件

- [ ] `frontend/src/types/card.ts` に `RefineCardRequest` と `RefineCardResponse` 型が追加されている
- [ ] `frontend/src/services/api.ts` に `refineCard()` メソッドが追加されている
- [ ] refineCard() が POST /cards/refine を呼び出し、適切なエラーハンドリングを行っている
- [ ] TypeScript 型チェック（tsc --noEmit）が通っている

---

## 主要実装項目

### TypeScript 型定義追加 🔵

**信頼性**: 🔵 *設計文書 architecture.md フロントエンドコンポーネント構成より*

`frontend/src/types/card.ts` に追加:
- `RefineCardRequest`: `{ front: string; back: string; language?: "ja" | "en" }`
- `RefineCardResponse`: `{ refined_front: string; refined_back: string; model_used: string; processing_time_ms: number }`

**実装ファイル**: `frontend/src/types/card.ts`

### API クライアントメソッド追加 🔵

**信頼性**: 🔵 *既存 ApiClient パターンより*

`frontend/src/services/api.ts` に追加:
- `refineCard(request: RefineCardRequest): Promise<RefineCardResponse>` メソッド
- POST /cards/refine への fetch 呼び出し
- AbortController による 15 秒タイムアウト制御
- エラーレスポンスのハンドリング

**実装ファイル**: `frontend/src/services/api.ts`

---

## 基本的なテスト要件

### 型チェック 🔵

**信頼性**: 🔵 *既存プロジェクトパターンより*

- tsc --noEmit が通ること（型の整合性確認）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0140` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 3 項目
- 🔵 **青信号**: 3 項目 (100%)
- 🟡 **黄信号**: 0 項目 (0%)
- 🔴 **赤信号**: 0 項目 (0%)

**品質評価**: ✅ 高品質
