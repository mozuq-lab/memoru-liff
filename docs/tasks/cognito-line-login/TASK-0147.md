# TASK-0147: app.ts LINE Login 環境変数設定

**タスクID**: TASK-0147
**タスクタイプ**: DIRECT
**推定工数**: 1時間
**フェーズ**: Phase 1 - CDK 実装
**信頼性レベル**: 🔵 *architecture.md「app.ts の更新」セクション・REQ-007 より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/cognito-line-login/requirements.md) - REQ-007
- **設計文書**: [architecture.md](../../design/cognito-line-login/architecture.md)

## タスク概要

`app.ts` の dev/prod 環境の `CognitoStack` インスタンス化に LINE Login の Props（`lineLoginChannelId`, `lineLoginChannelSecret`）を追加する。Channel ID/Secret は `process.env` 経由で注入し、CDK コード内にはハードコードしない。

## 依存タスク

- **前提タスク**: [TASK-0146: CognitoStack LINE Login 外部 IdP 実装](TASK-0146.md)
- **後続タスク**: なし

## 完了条件

- [ ] dev 環境の `CognitoStack` に `lineLoginChannelId` と `lineLoginChannelSecret` が Props として渡されている
- [ ] prod 環境の `CognitoStack` に `lineLoginChannelId` と `lineLoginChannelSecret` が Props として渡されている
- [ ] Channel ID/Secret は `process.env` から取得し、ハードコードしていない
- [ ] `npm run build` が成功する

---

## 主要実装項目

### 1. dev 環境の Props 追加 🔵

**信頼性**: 🔵 *architecture.md「app.ts の更新」セクション・REQ-007・既存 app.ts パターンより*

**実装ファイル**: `infrastructure/cdk/bin/app.ts`

```typescript
new CognitoStack(app, 'MemoruCognitoDev', {
  environment: 'dev',
  cognitoDomainPrefix: 'memoru-dev',
  callbackUrls: ['http://localhost:3000/callback', 'https://localhost:3000/callback'],
  logoutUrls: ['http://localhost:3000/', 'https://localhost:3000/'],
  lineLoginChannelId: process.env.LINE_LOGIN_CHANNEL_ID,
  lineLoginChannelSecret: process.env.LINE_LOGIN_CHANNEL_SECRET,
});
```

### 2. prod 環境の Props 追加 🔵

**信頼性**: 🔵 *既存 app.ts の dev/prod パターンより*

**実装ファイル**: `infrastructure/cdk/bin/app.ts`

```typescript
new CognitoStack(app, 'MemoruCognitoProd', {
  environment: 'prod',
  cognitoDomainPrefix: 'memoru-prod',
  callbackUrls: ['https://liff.example.com/callback'],
  logoutUrls: ['https://liff.example.com/'],
  lineLoginChannelId: process.env.LINE_LOGIN_CHANNEL_ID,
  lineLoginChannelSecret: process.env.LINE_LOGIN_CHANNEL_SECRET,
});
```

**注意**: dev/prod で異なる LINE Login チャネルを使用する場合は、環境変数名を分ける必要がある（例: `LINE_LOGIN_CHANNEL_ID_DEV` / `LINE_LOGIN_CHANNEL_ID_PROD`）。初期実装では共通の環境変数を使用し、必要に応じて分離する。

---

## 基本的なテスト要件

### ビルド確認 🔵

**信頼性**: 🔵 *既存ビルドプロセスより*

- `npm run build` が TypeScript コンパイルエラーなく成功すること
- TASK-0146 で追加した `CognitoStackProps` の型に適合していること

---

## 実装手順

### DIRECT プロセス

1. `/tsumiki:direct-setup` - app.ts の Props 追加
2. `/tsumiki:direct-verify` - ビルド確認

---

## 注意事項

- `process.env.LINE_LOGIN_CHANNEL_ID` が `undefined` の場合、Props は `undefined` となり、TASK-0146 の条件分岐で LINE IdP は作成されない（意図した動作）
- 本番デプロイ前に LINE Developers Console でチャネル作成と Callback URL 設定が必要（CDK スコープ外、デプロイ手順書 Step 3 参照）

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
