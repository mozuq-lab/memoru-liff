# TASK-0172: useStudyNotes カスタムフック

**タスクID**: TASK-0172
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🟡 *interfaces.ts設計・既存Reactフックパターンから妥当な推測*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/auto-study-notes/requirements.md) - REQ-ASN-201
- **型定義**: [📝 interfaces.ts](../../design/auto-study-notes/interfaces.ts)

## タスク概要

`frontend/src/hooks/useStudyNotes.ts` に要約ノートの取得・生成・状態管理を行うカスタムフックを実装する。

## 依存タスク

- **前提タスク**: [TASK-0171: TypeScript型定義 & APIサービス](TASK-0171.md)
- **後続タスク**: [TASK-0173: StudyNotesSection コンポーネント](TASK-0173.md)

## 完了条件

- [ ] `useStudyNotes(sourceType, sourceId)` フックが実装されている
- [ ] 状態管理（idle, loading, generating, cached, stale, empty, error）が正しい
- [ ] `generate()` 関数で要約生成をトリガーできる
- [ ] `refresh()` 関数でキャッシュ再取得できる
- [ ] ローディング状態とエラー状態が適切に管理される
- [ ] 単体テストが通る

---

## 実装詳細

### 1. useStudyNotes フック 🟡

**信頼性**: 🟡 *interfaces.ts設計・既存フックパターンから妥当な推測*

```typescript
export function useStudyNotes(sourceType: SourceType, sourceId: string): UseStudyNotesReturn {
  const [status, setStatus] = useState<StudyNotesStatus>("idle");
  const [data, setData] = useState<StudyNotesData | null>(null);
  const [error, setError] = useState<string | null>(null);

  // 初回マウント時にキャッシュ確認
  useEffect(() => {
    fetchCached();
  }, [sourceType, sourceId]);

  const generate = async () => { /* ... */ };
  const refresh = async () => { /* ... */ };

  return { status, data, error, generate, refresh };
}
```

**実装ファイル**: `frontend/src/hooks/useStudyNotes.ts`

### 2. 状態遷移 🟡

**信頼性**: 🟡 *dataflow.md のフロントエンド状態管理図から妥当な推測*

idle → loading → (cached | stale | empty | error)
empty → generating → (cached | error)
stale → generating → (cached | error)

---

## 単体テスト要件

### テストケース1: 初回ロード（キャッシュヒット） 🟡

**Given**: キャッシュ済みのデッキ
**When**: フックをマウント
**Then**: status が "loading" → "cached" に遷移

### テストケース2: 初回ロード（キャッシュなし） 🟡

**Given**: キャッシュなしのデッキ
**When**: フックをマウント
**Then**: status が "loading" → "empty" に遷移

### テストケース3: 生成トリガー 🟡

**Given**: status が "empty"
**When**: generate() を呼び出し
**Then**: status が "generating" → "cached" に遷移

### テストケース4: エラー処理 🟡

**Given**: API呼び出しが失敗
**When**: generate() を呼び出し
**Then**: status が "error" になり、error にメッセージがセットされる

**テストファイル**: `frontend/src/hooks/__tests__/useStudyNotes.test.ts`

---

## 実装手順

1. `/tsumiki:tdd-red` - テスト実装（失敗）
2. `/tsumiki:tdd-green` - 最小実装
3. `/tsumiki:tdd-refactor` - リファクタリング

---

## 信頼性レベルサマリー

- **総項目数**: 6項目
- 🔵 **青信号**: 0項目 (0%)
- 🟡 **黄信号**: 6項目 (100%)

**品質評価**: ⚠️ 要改善（フック設計の詳細は実装時に調整が必要）
