# TASK-0162: StudyNotesResult データクラス & AIService Protocol 拡張

**タスクID**: TASK-0162
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *REQ-ASN-401・既存AIService Protocol設計より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/auto-study-notes/requirements.md) - REQ-ASN-401
- **設計文書**: [📐 architecture.md](../../design/auto-study-notes/architecture.md)
- **型定義**: [📝 interfaces.py](../../design/auto-study-notes/interfaces.py)

## タスク概要

`backend/src/services/ai_service.py` に `StudyNotesResult` データクラスと `CardForNotes` データクラスを追加し、`AIService` Protocol に `generate_study_notes()` メソッドを追加する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0163: Pydantic リクエスト/レスポンスモデル](TASK-0163.md), [TASK-0167: AIService generate_study_notes() 実装](TASK-0167.md)

## 完了条件

- [ ] `StudyNotesResult` データクラスが定義されている（content, model_used, processing_time_ms）
- [ ] `CardForNotes` データクラスが定義されている（front, back, tags）
- [ ] `AIService` Protocol に `generate_study_notes(cards: List[CardForNotes]) -> StudyNotesResult` が追加されている
- [ ] 既存のテストが全て通る
- [ ] 新規データクラスの単体テストが通る

---

## 実装詳細

### 1. StudyNotesResult データクラス 🔵

**信頼性**: 🔵 *interfaces.py設計・既存GenerationResultパターンより*

```python
@dataclass
class StudyNotesResult:
    """要約ノート生成結果."""
    content: str
    model_used: str
    processing_time_ms: int
```

**実装ファイル**: `backend/src/services/ai_service.py`

### 2. CardForNotes データクラス 🔵

**信頼性**: 🔵 *interfaces.py設計・既存Cardモデルより*

```python
@dataclass
class CardForNotes:
    """要約ノート生成用のカード情報."""
    front: str
    back: str
    tags: List[str] = field(default_factory=list)
```

**実装ファイル**: `backend/src/services/ai_service.py`

### 3. AIService Protocol 拡張 🔵

**信頼性**: 🔵 *REQ-ASN-401・既存Protocolパターンより*

```python
def generate_study_notes(
    self,
    cards: List[CardForNotes],
) -> StudyNotesResult:
    """カード群から要約ノートを生成する."""
    ...
```

**実装ファイル**: `backend/src/services/ai_service.py`

---

## 単体テスト要件

### テストケース1: StudyNotesResult の生成 🔵

**信頼性**: 🔵 *既存データクラステストパターンより*

**Given**: content, model_used, processing_time_ms の値
**When**: StudyNotesResult を生成
**Then**: 各フィールドが正しく設定される

### テストケース2: CardForNotes の生成 🔵

**Given**: front, back, tags の値
**When**: CardForNotes を生成
**Then**: 各フィールドが正しく設定される（tags のデフォルトは空リスト）

### テストケース3: AIService Protocol 互換性 🔵

**Given**: 既存のBedrockService / StrandsAIService
**When**: runtime_checkable でチェック
**Then**: 新メソッド追加後も既存実装がProtocolに適合する（NotImplementedErrorでOK）

**テストファイル**: `backend/tests/unit/test_ai_service.py`

---

## 実装手順

1. `/tsumiki:tdd-red` - テスト実装（失敗）
2. `/tsumiki:tdd-green` - 最小実装
3. `/tsumiki:tdd-refactor` - リファクタリング

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 3 | 0 | 0 | 3 |

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)

**品質評価**: ✅ 高品質
