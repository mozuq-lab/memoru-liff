# TASK-0120: from_dynamodb_item デフォルト settings 修正

## タスク概要

- **タスクタイプ**: DIRECT
- **推定工数**: 0.25h
- **依存タスク**: なし
- **関連要件**: REQ-102
- **関連設計**: [architecture.md](../../design/day-boundary-fix/architecture.md) §3

## 実装ファイル

- `backend/src/models/user.py`

## 実装詳細

### user.py: `User.from_dynamodb_item()` のデフォルト settings に `day_start_hour` を追加

**現状** (line ~146):
```python
settings=item.get("settings", {
    "notification_time": "09:00",
    "timezone": "Asia/Tokyo",
}),
```

**修正後**:
```python
settings=item.get("settings", {
    "notification_time": "09:00",
    "timezone": "Asia/Tokyo",
    "day_start_hour": 4,
}),
```

`User` モデルの `default_factory` (line ~100) には既に `day_start_hour: 4` が含まれているため、`from_dynamodb_item` のフォールバック辞書との一貫性を確保する。

## 完了条件

- [x] `from_dynamodb_item` のデフォルト settings に `"day_start_hour": 4` が含まれている
- [x] `User` モデルの `default_factory` と `from_dynamodb_item` のデフォルトが一致している
- [x] 既存テストが全て通る
