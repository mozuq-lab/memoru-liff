# TASK-0118: ZoneInfo フォールバック実装

## タスク概要

- **タスクタイプ**: TDD
- **推定工数**: 0.5h
- **依存タスク**: TASK-0116
- **関連要件**: REQ-003
- **関連設計**: [architecture.md](../../design/day-boundary-fix/architecture.md) §1, [dataflow.md](../../design/day-boundary-fix/dataflow.md) §修正後のレビュー送信フロー

## 実装ファイル

- `backend/src/services/srs.py` — 実装
- `backend/tests/unit/test_srs.py` — テスト

## 実装詳細

### srs.py: `calculate_next_review_boundary()` に ZoneInfo フォールバック追加

**現状** (line ~105):
```python
user_tz = ZoneInfo(user_timezone)
```

**修正後**:
```python
try:
    user_tz = ZoneInfo(user_timezone)
except (ZoneInfoNotFoundError, KeyError):
    logger.warning(f"Invalid timezone '{user_timezone}', falling back to Asia/Tokyo")
    user_tz = ZoneInfo("Asia/Tokyo")
```

### test_srs.py: テストケース追加

```python
def test_invalid_timezone_fallback():
    """不正 TZ で Asia/Tokyo にフォールバックすること"""

def test_fallback_produces_valid_result():
    """フォールバック時の結果が正しいこと（Asia/Tokyo 相当）"""
```

## 完了条件

- [ ] 不正 timezone で `ZoneInfoNotFoundError` / `KeyError` が catch される
- [ ] フォールバック時に `logger.warning` でログが出力される
- [ ] フォールバック時に `Asia/Tokyo` が使用される
- [ ] 不正 TZ フォールバックのテストが通る
- [ ] フォールバック結果の正確性テストが通る
- [ ] 既存テストが全て通る
