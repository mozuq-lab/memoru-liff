# TASK-0117: Decimal→int 正規化の実装

## タスク概要

- **タスクタイプ**: TDD
- **推定工数**: 0.5h
- **依存タスク**: TASK-0116
- **関連要件**: REQ-001
- **関連設計**: [architecture.md](../../design/day-boundary-fix/architecture.md) §1

## 実装ファイル

- `backend/src/services/srs.py` — 実装
- `backend/tests/unit/test_srs.py` — テスト

## 実装詳細

### srs.py: `calculate_next_review_boundary()` に型変換を追加

**現状**: `interval` と `day_start_hour` をそのまま使用。DynamoDB から `Decimal` が来ると `datetime()` で `TypeError` 発生。

**修正後** (関数冒頭):
```python
def calculate_next_review_boundary(
    interval: int,
    user_timezone: str = "Asia/Tokyo",
    day_start_hour: int = 4,
) -> datetime:
    interval = int(interval)
    day_start_hour = int(day_start_hour)
    if not 0 <= day_start_hour <= 23:
        raise ValueError(f"day_start_hour must be 0-23, got {day_start_hour}")
    # ... 以降は既存ロジック
```

### test_srs.py: テストケース追加

```python
def test_decimal_day_start_hour():
    """Decimal(4) を渡しても int(4) と同じ結果になること"""

def test_decimal_boundary_values():
    """Decimal(0) / Decimal(23) の境界値テスト"""

def test_invalid_day_start_hour_range():
    """Decimal(24) / Decimal(-1) で ValueError"""
```

## 完了条件

- [x] `int(interval)` と `int(day_start_hour)` が関数冒頭で実行される
- [x] `day_start_hour` が 0-23 の範囲外なら `ValueError` が raise される
- [x] `Decimal(4)` を `day_start_hour` に渡したテストが通る
- [x] `Decimal(0)`, `Decimal(23)` の境界値テストが通る
- [x] `Decimal(24)`, `Decimal(-1)` で `ValueError` テストが通る
- [x] 既存テストが全て通る
