# TASK-0119: validate_timezone ZoneInfo 検証

## タスク概要

- **タスクタイプ**: TDD
- **推定工数**: 0.5h
- **依存タスク**: なし
- **関連要件**: REQ-002, REQ-103
- **関連設計**: [architecture.md](../../design/day-boundary-fix/architecture.md) §2

## 実装ファイル

- `backend/src/models/user.py` — 実装
- `backend/tests/unit/test_user_models.py` — テスト

## 実装詳細

### user.py: `validate_timezone()` を ZoneInfo 検証に置き換え

**現状** (lines 40-59):
```python
@field_validator("timezone")
@classmethod
def validate_timezone(cls, v: Optional[str]) -> Optional[str]:
    if v is None:
        return v
    valid_timezones = {
        "Asia/Tokyo", "America/New_York", ...
    }
    if not re.match(r"^[A-Za-z_]+/[A-Za-z_]+$", v) and v != "UTC":
        raise ValueError("Invalid timezone format.")
    return v
```

**修正後**:
```python
@field_validator("timezone")
@classmethod
def validate_timezone(cls, v: Optional[str]) -> Optional[str]:
    """Validate IANA timezone string using ZoneInfo."""
    if v is None:
        return v
    from zoneinfo import ZoneInfo, ZoneInfoNotFoundError
    try:
        ZoneInfo(v)
    except (ZoneInfoNotFoundError, KeyError):
        raise ValueError(f"Invalid timezone: {v}")
    return v
```

- 正規表現と `valid_timezones` セットを削除
- `ZoneInfo` による実在検証に置き換え
- `re` import が不要になった場合は削除（他で使用していなければ）

### test_user_models.py: テストケース追加

```python
def test_invalid_timezone_non_existent():
    """非実在 TZ "Foo/Bar" が拒否されること"""

def test_valid_timezone_zoneinfo():
    """有効な TZ "Asia/Tokyo", "UTC" が受け入れられること"""
```

## 完了条件

- [ ] `validate_timezone` が `ZoneInfo` で実在検証を行う
- [ ] 正規表現バリデーションが削除されている
- [ ] `"Foo/Bar"` で `ValidationError` が発生するテストが通る
- [ ] `"Asia/Tokyo"`, `"UTC"` が受け入れられるテストが通る
- [ ] 既存テストが全て通る
