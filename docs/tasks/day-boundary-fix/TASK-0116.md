# TASK-0116: srs.py import 移動と Logger 追加

## タスク概要

- **タスクタイプ**: DIRECT
- **推定工数**: 0.25h
- **依存タスク**: なし
- **関連要件**: REQ-101（import 移動）, REQ-003（Logger 追加）
- **関連設計**: [architecture.md](../../design/day-boundary-fix/architecture.md) §1

## 実装ファイル

- `backend/src/services/srs.py`

## 実装詳細

### 変更1: `zoneinfo` import をモジュールレベルに移動

**現状** (line 103):
```python
def calculate_next_review_boundary(...):
    from zoneinfo import ZoneInfo
```

**修正後** (モジュール先頭):
```python
from zoneinfo import ZoneInfo, ZoneInfoNotFoundError
```

関数内の `from zoneinfo import ZoneInfo` を削除。

### 変更2: Logger インスタンスの追加

**修正後** (モジュールレベル):
```python
from aws_lambda_powertools import Logger

logger = Logger()
```

TASK-0118（ZoneInfo フォールバック）で使用するログ出力の準備。

## 完了条件

- [x] `from zoneinfo import ZoneInfo, ZoneInfoNotFoundError` がモジュールレベルにある
- [x] 関数内の `from zoneinfo import ZoneInfo` が削除されている
- [x] `aws_lambda_powertools.Logger` がインポートされ `logger = Logger()` がモジュールレベルにある
- [x] 既存テストが全て通る
