# TASK-0133: 旧 CloudFormation テンプレート削除

**タスクID**: TASK-0133
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - CDK 移行実装
**信頼性レベル**: 🔵 *要件定義 REQ-006・ヒアリングより*

## 関連文書

- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/infra-cdk-migration/requirements.md) - REQ-006, REQ-403
- **設計文書**: [architecture.md](../../design/infra-cdk-migration/architecture.md)

## タスク概要

CDK 実装が完了し動作確認が取れた後、旧 CloudFormation テンプレートを削除する。`infrastructure/keycloak/realm-local.json` と `test-users.json` はローカル開発用のため残す。

## 依存タスク

- **前提タスク**: [TASK-0132: cdk synth 検証・ドキュメント更新](TASK-0132.md)
- **後続タスク**: なし

## 完了条件

- [ ] `infrastructure/keycloak/template.yaml` が削除されている
- [ ] `infrastructure/liff-hosting/template.yaml` が削除されている
- [ ] `infrastructure/cognito/template.yaml` が削除されている
- [ ] `infrastructure/keycloak/realm-local.json` が残っている（削除しない）
- [ ] `infrastructure/keycloak/test-users.json` が残っている（削除しない）
- [ ] `infrastructure/liff-hosting/` ディレクトリが削除されている（template.yaml のみだったため）
- [ ] CDK プロジェクトの `npm run build` がエラーなく完了する

---

## 主要実装項目

### 1. 旧テンプレートファイルの削除 🔵

**信頼性**: 🔵 *要件定義 REQ-006・ヒアリング Q4 より*

以下のファイルを削除:
- `infrastructure/keycloak/template.yaml`
- `infrastructure/liff-hosting/template.yaml`
- `infrastructure/cognito/template.yaml`

### 2. 空ディレクトリの整理 🔵

**信頼性**: 🔵 *note.md プロジェクト構成より*

- `infrastructure/liff-hosting/` — template.yaml のみだったためディレクトリごと削除
- `infrastructure/cognito/` — template.yaml のみだったためディレクトリごと削除
- `infrastructure/keycloak/` — `realm-local.json` と `test-users.json` が残るため存続

### 3. 残留ファイルの確認 🔵

**信頼性**: 🔵 *要件定義 REQ-403 より*

以下のファイルが残っていることを確認:
- `infrastructure/keycloak/realm-local.json`（ローカル開発用）
- `infrastructure/keycloak/test-users.json`（ローカル開発用）

---

## 基本的なテスト要件

### 削除後の整合性確認 🔵

**信頼性**: 🔵 *プロジェクト構成の整合性確認*

- 旧テンプレートが削除されていること
- CDK プロジェクトのビルドに影響がないこと
- ローカル開発用ファイルが残っていること

---

## 注意事項

- このタスクは CDK 実装の動作確認が完了してから実行すること
- git で削除をコミットするため、旧テンプレートは git 履歴から復元可能

---

## 実装手順

1. `/tsumiki:direct-setup` - ファイル削除・ディレクトリ整理
2. `/tsumiki:direct-verify` - 削除後の確認

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
