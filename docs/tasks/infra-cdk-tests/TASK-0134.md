# TASK-0134: CognitoStack テスト作成

**タスクID**: TASK-0134
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - CDK テスト実装
**信頼性レベル**: 🔵 *要件定義 REQ-001〜REQ-005、既存 cognito-stack.ts より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/infra-cdk-tests/requirements.md) - REQ-001, REQ-002, REQ-003, REQ-005
- **設計文書**: [architecture.md](../../design/infra-cdk-tests/architecture.md)

## タスク概要

`infrastructure/cdk/test/cognito-stack.test.ts` を作成し、CognitoStack の Snapshot テスト（dev/prod）、環境差異の Fine-grained assertions、セキュリティ設定検証、OIDC 設定検証を実装する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: なし

## 完了条件

- [ ] `infrastructure/cdk/test/cognito-stack.test.ts` が作成されている
- [ ] dev 環境の Snapshot テストが Pass する
- [ ] prod 環境の Snapshot テストが Pass する
- [ ] 環境差異 assertions（deletionProtection, removalPolicy）が Pass する
- [ ] セキュリティ assertions（パスワードポリシー, MFA）が Pass する
- [ ] OIDC assertions（OAuth flows, scopes）が Pass する
- [ ] `cd infrastructure/cdk && npm test` で全テストが Pass する

---

## 主要実装項目

### 1. テスト用 Props 定義 🔵

**信頼性**: 🔵 *既存 cognito-stack.ts の CognitoStackProps インターフェースより*

dev/prod の Props を定義する。

```typescript
const devProps: CognitoStackProps = {
  environment: 'dev',
  cognitoDomainPrefix: 'memoru-dev-test',
  callbackUrls: ['http://localhost:3000/callback'],
  logoutUrls: ['http://localhost:3000/'],
};

const prodProps: CognitoStackProps = {
  environment: 'prod',
  cognitoDomainPrefix: 'memoru-prod-test',
  callbackUrls: ['https://app.example.com/callback'],
  logoutUrls: ['https://app.example.com/'],
};
```

**実装ファイル**: `infrastructure/cdk/test/cognito-stack.test.ts`

### 2. Snapshot テスト（dev/prod） 🔵

**信頼性**: 🔵 *REQ-001, REQ-002、CDK assertions 標準パターンより*

```typescript
const template = Template.fromStack(stack);
expect(template.toJSON()).toMatchSnapshot();
```

### 3. 環境差異 assertions 🔵

**信頼性**: 🔵 *REQ-003、cognito-stack.ts L45-46 の isProd パターンより*

- dev: `DeletionProtection: false`、`UpdateReplacePolicy: Delete`
- prod: `DeletionProtection: true`、`UpdateReplacePolicy: Retain`

### 4. セキュリティ assertions 🔵

**信頼性**: 🔵 *REQ-005、cognito-stack.ts L36-42 より*

- パスワードポリシー: `MinimumLength: 8`, `RequireLowercase: true`, `RequireUppercase: true`, `RequireNumbers: true`, `RequireSymbols: true`
- MFA: `MfaConfiguration: 'OPTIONAL'`、`EnabledMfas: ['SOFTWARE_TOKEN_MFA']`

### 5. OIDC assertions 🔵

**信頼性**: 🔵 *REQ-005、cognito-stack.ts L64-66 より*

- OAuth flows: `AuthorizationCodeGrant: true`
- Scopes: openid, profile, email

---

## 基本的なテスト要件

### Snapshot テスト 🔵

**信頼性**: 🔵 *REQ-001, REQ-002 より*

- dev Props で synth した CloudFormation テンプレートが Snapshot と一致
- prod Props で synth した CloudFormation テンプレートが Snapshot と一致

### Fine-grained assertions 🔵

**信頼性**: 🔵 *REQ-003, REQ-005 より*

- `AWS::Cognito::UserPool` の DeletionProtection が dev/prod で異なる
- `AWS::Cognito::UserPool` のパスワードポリシーが要件通り
- `AWS::Cognito::UserPool` の MFA 設定が OPTIONAL + TOTP
- `AWS::Cognito::UserPoolClient` の OAuth フローと scopes が正しい

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0134` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
