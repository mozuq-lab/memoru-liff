# TASK-0103: ユーザー設定モデル拡張 (day_start_hour)

**タスクID**: TASK-0103
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - バックエンド実装
**信頼性レベル**: 🔵 *architecture.md コンポーネント設計2, 6より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/day-boundary/requirements.md) - REQ-002
- **設計文書**: [📐 architecture.md](../../design/day-boundary/architecture.md)

## タスク概要

ユーザー設定に `day_start_hour` を追加する。モデル、サービス、APIハンドラーの3層を変更。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0104: レビューフロー正規化統合](TASK-0104.md), [TASK-0105: カード interval 更新の正規化](TASK-0105.md), [TASK-0106: フロントエンド設定 UI](TASK-0106.md)

## 完了条件

- [ ] `UserSettingsRequest` に day_start_hour バリデーション追加
- [ ] `UserResponse` に day_start_hour フィールド追加
- [ ] `User.settings` デフォルトに day_start_hour 追加
- [ ] `user_service.update_settings()` が day_start_hour を保存できる
- [ ] `user_handler` が day_start_hour を受け付ける
- [ ] 単体テストが全て通る

---

## 主要実装項目

### 1. `UserSettingsRequest` に `day_start_hour` 追加 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計2より*

`UserSettingsRequest` に `day_start_hour: Optional[int]` を追加（0-23 バリデーション）。

**実装ファイル**: `backend/src/models/user.py`

### 2. `UserResponse` に `day_start_hour` 追加 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計2より*

`UserResponse` に `day_start_hour: int = 4` を追加。

**実装ファイル**: `backend/src/models/user.py`

### 3. `User.settings` デフォルトに `day_start_hour` 追加 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計2より*

`User.settings` デフォルトに `"day_start_hour": 4` を追加。

**実装ファイル**: `backend/src/models/user.py`

### 4. `User.to_response()` に `day_start_hour` フィールド追加 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計2より*

`User.to_response()` で `day_start_hour` フィールドを返すようにする。

**実装ファイル**: `backend/src/models/user.py`

### 5. `user_service.update_settings()` に `day_start_hour` パラメータ追加 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計6より*

`user_service.update_settings()` に `day_start_hour` パラメータを追加。

**実装ファイル**: `backend/src/services/user_service.py`

### 6. `user_handler.update_settings()` で `day_start_hour` を渡す 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計6より*

`user_handler.update_settings()` で `day_start_hour` を `user_service` に渡す。

**実装ファイル**: `backend/src/api/handlers/user_handler.py`

---

## 基本的なテスト要件

### バリデーションテスト 🔵

**信頼性**: 🔵 *REQ-002 範囲0-23より*

- day_start_hour のバリデーション（0-23 範囲外でエラー） 🔵

### 設定保存・取得テスト 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計2より*

- 設定の保存と取得 🔵

### デフォルト値テスト 🔵

**信頼性**: 🔵 *architecture.md コンポーネント設計2より*

- デフォルト値 4 の適用（既存ユーザーで未設定の場合） 🔵

**テストファイル**: `backend/tests/unit/models/test_user.py`, `backend/tests/unit/services/test_user_service.py`

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0103` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 既存の notification_time / timezone と同じパターンで実装
- DynamoDB の settings Map に追加するだけ（スキーマ変更不要）

---

## 信頼性レベルサマリー

- **総項目数**: 9項目
- 🔵 **青信号**: 9項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
