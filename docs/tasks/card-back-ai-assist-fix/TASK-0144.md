# TASK-0144: CardForm useEffect cleanup + テスト

**タスクID**: TASK-0144
**タスクタイプ**: TDD
**推定工数**: 1時間
**フェーズ**: Phase 1 - レビュー修正
**信頼性レベル**: 🔵 *レビュー指摘 #4・設計文書 architecture.md・React ベストプラクティスより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/card-back-ai-assist-fix/requirements.md) - REQ-FIX-005, REQ-TEST-005
- **設計文書**: [📐 architecture.md](../../design/card-back-ai-assist-fix/architecture.md)

## タスク概要

`CardForm` コンポーネントにアンマウント時の `useEffect` cleanup を追加し、進行中の AI 補足リクエストをキャンセルする。`abortControllerRef.current?.abort()` を cleanup 関数で呼び出す。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: なし（独立タスク）

## 完了条件

- [x] `CardForm` に `useEffect` cleanup（`abortControllerRef.current?.abort()`）が追加済み
- [x] アンマウント時キャンセルテスト 1件が追加され全パス

---

## 主要実装項目

### 1. useEffect cleanup 追加 🔵

**信頼性**: 🔵 *設計文書 architecture.md「修正 3」・React ベストプラクティスより*

```tsx
useEffect(() => {
  return () => {
    abortControllerRef.current?.abort();
  };
}, []);
```

**実装ファイル**: `frontend/src/components/CardForm.tsx`

### 2. テスト追加 🟡

**信頼性**: 🟡 *REQ-TEST-005・React Testing Library のアンマウント検証パターンから妥当な推測*

アンマウント時に `AbortController.abort()` が呼ばれることを検証するテスト。

**テストファイル**: `frontend/src/components/__tests__/CardForm.test.tsx`

---

## 基本的なテスト要件

### アンマウント時キャンセルテスト 🟡

**信頼性**: 🟡 *React Testing Library パターンから妥当な推測*

- AI 補足リクエスト進行中にコンポーネントをアンマウント → `abort()` が呼ばれる

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0144` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `useRef` の `abortControllerRef` は既存実装で定義済み
- 既存の `signal.aborted` チェックロジックは維持する
- 外部インターフェースへの変更なし

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 1項目 (50%)
- 🟡 **黄信号**: 1項目 (50%) — テスト実装パターン
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
