# TASK-0145: BedrockService refine テスト追加

**タスクID**: TASK-0145
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - レビュー修正
**信頼性レベル**: 🔵 *レビュー指摘 #6・設計文書 architecture.md・既存 test_strands_refine.py パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/card-back-ai-assist-fix/requirements.md) - REQ-TEST-004
- **設計文書**: [📐 architecture.md](../../design/card-back-ai-assist-fix/architecture.md)

## タスク概要

`BedrockService.refine_card()` の正常系・パース系・エラー系テストを新規ファイル `test_bedrock_refine.py` に追加する。既存の `test_strands_refine.py` と対称的なテストカバレッジを実現する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: なし（独立タスク）

## 完了条件

- [x] `test_bedrock_refine.py` が新規作成済み
- [x] 正常系テスト 2件（both / front_only）が追加され全パス
- [x] パース系テスト 2件（markdown JSON / missing field）が追加され全パス
- [x] エラー系テスト 1件（timeout）が追加され全パス

---

## 主要実装項目

### 1. 正常系テスト 🔵

**信頼性**: 🔵 *設計文書 architecture.md「test_bedrock_refine.py」・既存 test_strands_refine.py パターンより*

`TestBedrockRefineCardSuccess` クラス:
- `test_refine_both` — 表面・裏面両方の refine が成功
- `test_refine_front_only` — 表面のみの refine が成功

Bedrock クライアント（`invoke_model`）を mock し、正しい JSON レスポンスを返すように設定。

**テストファイル**: `backend/tests/unit/test_bedrock_refine.py`

### 2. パース系テスト 🔵

**信頼性**: 🔵 *設計文書 architecture.md「TestBedrockRefineCardParsing」セクションより*

`TestBedrockRefineCardParsing` クラス:
- `test_markdown_json_response` — Markdown コードブロック内 JSON がパースされること
- `test_missing_field_raises_error` — 必須フィールド欠落で `BedrockParseError`

**テストファイル**: `backend/tests/unit/test_bedrock_refine.py`

### 3. エラー系テスト 🔵

**信頼性**: 🔵 *設計文書 architecture.md「TestBedrockRefineCardErrors」セクションより*

`TestBedrockRefineCardErrors` クラス:
- `test_timeout_raises_error` — タイムアウト時に `BedrockTimeoutError`

**テストファイル**: `backend/tests/unit/test_bedrock_refine.py`

---

## 基本的なテスト要件

### BedrockService refine テスト 🔵

**信頼性**: 🔵 *REQ-TEST-004・test_strands_refine.py 対称パターンより*

- 正常系: 両方入力 → `RefineResult` 返却
- 正常系: 表面のみ → `RefineResult` 返却
- パース系: Markdown コードブロック JSON → 正常パース
- パース系: 必須フィールド欠落 → `BedrockParseError`
- エラー系: タイムアウト → `BedrockTimeoutError`

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0145` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `test_strands_refine.py` のテストパターン（fixture、mock 方式）を参考にする
- BedrockService は `boto3` クライアントを使用するため、`invoke_model` を mock する
- 既存の `BedrockService` のエラーハンドリング（`_parse_refine_result` 等）をテストで検証

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
