# TASK-0156: SAM template 更新 + 統合テスト

**タスクID**: TASK-0156
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 3 - 統合
**信頼性レベル**: 🔵 *要件定義書・設計文書 architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/learning-stats-dashboard/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/learning-stats-dashboard/architecture.md)

## タスク概要

SAM template.yaml に学習統計 API のルート定義（GET /stats, GET /stats/weak-cards, GET /stats/forecast）を追加し、バックエンドからフロントエンドまでの統合テストを作成する。

## 依存タスク

- **前提タスク**:
  - [TASK-0152: バックエンド stats_handler + handler.py 登録](TASK-0152.md)
  - [TASK-0155: ナビゲーション統合 + ルーティング](TASK-0155.md)
- **後続タスク**: なし

## 完了条件

- [x] `backend/template.yaml` に GET /stats ルートが追加されている
- [x] `backend/template.yaml` に GET /stats/weak-cards ルートが追加されている
- [x] `backend/template.yaml` に GET /stats/forecast ルートが追加されている
- [x] 統合テストで API エンドポイントの動作が確認できる
- [x] 既存テストが全て通る

---

## 主要実装項目

### 1. SAM template.yaml ルート追加 🔵

**信頼性**: 🔵 *既存 template.yaml のルート定義パターンより*

API Gateway の Events セクションに以下のルートを追加:
- `GetStats`: GET /stats
- `GetWeakCards`: GET /stats/weak-cards
- `GetForecast`: GET /stats/forecast

既存のルート定義パターン（認証設定、Lambda 関数紐付け等）に合わせる。

**実装ファイル**: `backend/template.yaml`

### 2. 統合テスト 🔵

**信頼性**: 🔵 *既存統合テストパターンより*

以下のフローを確認する統合テストを作成:
- 認証済みユーザーで GET /stats が正常レスポンスを返すこと
- 認証済みユーザーで GET /stats/weak-cards が正常レスポンスを返すこと
- 認証済みユーザーで GET /stats/forecast が正常レスポンスを返すこと
- カードデータがある状態で統計値が正しく計算されること

**実装ファイル**: `backend/tests/integration/test_stats_integration.py`

---

## 基本的なテスト要件

### SAM template ルート定義テスト 🔵

**信頼性**: 🔵 *既存テストパターンより*

- template.yaml が正しくパースできること
- 新規ルートが API Gateway イベントとして定義されていること

### 統合テスト 🔵

**信頼性**: 🔵 *既存統合テストパターンより*

- 全3エンドポイントのエンドツーエンドテスト
- 認証なしリクエストの拒否確認
- 空データ時の正常レスポンス確認

---

## 実装手順

1. `/tsumiki:direct-setup` - SAM template 更新 + 統合テスト作成
2. `/tsumiki:direct-verify` - 動作確認

---

## 注意事項

- SAM template の変更は既存エンドポイントに影響しないよう注意する
- 統合テストはモックを最小限にし、可能な限り実際の処理フローを通す
- デプロイはユーザーが手動で実行する

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
