# TASK-0161: 依存パッケージ追加と環境設定

**タスクID**: TASK-0161
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *REQ-401・feature-backlog.md 前提条件より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/agentcore-memory-integration/requirements.md) - REQ-401
- **設計文書**: [📐 architecture.md](../../design/agentcore-memory-integration/architecture.md)

## タスク概要

AgentCore Memory 統合に必要な `bedrock-agentcore-sdk-python` パッケージを依存関係に追加し、ローカル開発環境での動作確認を行う。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0162: SAM テンプレート更新](TASK-0162.md)

## 完了条件

- [ ] `bedrock-agentcore[strands-agents]` が `backend/requirements.txt` と `backend/src/requirements.txt` に追加されている
- [ ] `pip install` でエラーなくインストールできる
- [ ] `from bedrock_agentcore.memory import AgentCoreMemoryClient` がインポート可能
- [ ] `from bedrock_agentcore.memory.integrations.strands import AgentCoreMemorySessionManager` がインポート可能
- [ ] 既存テストが全て通る（退行なし）

---

## 実装詳細

### 1. requirements.txt への追加 🔵

**信頼性**: 🔵 *REQ-401・feature-backlog.md 前提条件より*

`backend/requirements.txt` と `backend/src/requirements.txt` に以下を追加:

```
bedrock-agentcore[strands-agents]>=0.1.0
```

**実装ファイル**: `backend/requirements.txt`, `backend/src/requirements.txt`

### 2. インポート確認 🔵

**信頼性**: 🔵 *AgentCore SDK ドキュメントより*

以下のインポートが正常に動作することを確認:

```python
from bedrock_agentcore.memory import AgentCoreMemoryClient
from bedrock_agentcore.memory.integrations.strands.session_manager import AgentCoreMemorySessionManager
from bedrock_agentcore.memory.integrations.strands.config import AgentCoreMemoryConfig
```

### 3. 既存テストの退行確認 🔵

**信頼性**: 🔵 *CLAUDE.md 開発ルールより*

```bash
cd backend && make test
```

---

## 基本的なテスト要件

### インポートテスト 🔵

**信頼性**: 🔵 *REQ-401より*

- `bedrock_agentcore` パッケージのインポートが成功すること
- 既存の `strands-agents` パッケージとの互換性があること

---

## 実装手順

1. `/tsumiki:direct-setup` - パッケージ追加・インストール
2. `/tsumiki:direct-verify` - インポート確認・既存テスト実行

---

## 注意事項

- `bedrock-agentcore[strands-agents]` は `strands-agents` パッケージのバージョンと互換性がある必要がある
- 既存の `strands-agents>=0.1.0,<2.0.0` との競合がないことを確認

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
