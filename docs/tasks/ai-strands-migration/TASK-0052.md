# TASK-0052: Strands Agents SDK 依存追加 + ビルド確認

**status**: [x] completed
**phase**: Phase 1 - 基盤構築
**type**: DIRECT
**hours_estimate**: 4
**hours_actual**: 0
**reliability_index**: 85 (4 🔵 / 1 🟡 / 0 🔴)

## 関連文書

- [ai-strands-migration 概要](../overview.md)
- [要件定義](../../spec/ai-strands-migration/requirements.md)
- [アーキテクチャ設計](../../design/ai-strands-migration/architecture.md)

## タスク概要

Strands Agents SDK をプロジェクトの依存パッケージに追加し、SAM ビルドが正常に動作することを確認する。

このタスクは Phase 1（基盤構築）の最初のステップであり、後続の SDK 統合実装の基盤を構築する。

## 依存タスク

**ブロックの対象**:
- TASK-0053: AIService Protocol + 共通型定義 + 例外階層
- TASK-0057: Strands Agents SDK 統合（grade_answer 実装）

## 完了条件

- [x] `backend/requirements.txt` に `strands-agents` がバージョンピン留めで追加されている
- [x] `sam build` コマンドが正常に完了する
- [x] Python で `from strands import Agent` が成功する
- [x] 既存テストスイート（260+ テスト）が全て通過する（REQ-SM-405）

## 実装詳細

### 1. 依存パッケージの追加

#### `backend/requirements.txt` の更新

現在の requirements.txt に以下を追加する（バージョンは最新安定版を選定）:

```
strands-agents>=0.1.0,<1.0.0
strands-agents-tools>=0.1.0,<1.0.0
```

**信頼性レベル**: 🔵 確定（要件 REQ-SM-001 より）
- Strands Agents SDK はコア依存で、AWS の正式版を採用
- バージョンピン留めにより破壊的変更からの保護

#### `backend/requirements-dev.txt` の確認

テスト用追加依存が必要な場合は以下を追加:
- `strands-agents-testing` があれば追加
- 既存テスト依存（pytest, pytest-cov など）との互換性を確認

**信頼性レベル**: 🟡 調査必要（SDK テスト依存の詳細が未確定）

### 2. SAM ビルド確認

```bash
cd backend
sam build
```

**期待される出力**:
- ビルド完了メッセージ
- `.aws-sam/build/` ディレクトリが生成される
- 全ての Lambda 関数が正常にパッケージされている

**信頼性レベル**: 🔵 確定（技術的な標準手順）

### 3. Python インポートテスト

```bash
cd backend
python -c "from strands import Agent; print('Strands SDK import successful')"
```

または、エンタープライズ版の場合:

```python
from strands.agents import Agent
```

**信頼性レベル**: 🔵 確定（技術的な標準手順）

### 4. 既存テストの実行

```bash
cd backend
make test
```

**期待**: 260+ テストが全て通過（既存テストへの副作用がないことを確認）

**信頼性レベル**: 🔵 確定（REQ-SM-405）

## 単体テスト要件

### テストケース 1: SDK インポートテスト

**目的**: Strands SDK が正常にインストールされ、インポート可能であることを確認

```python
# backend/tests/unit/test_strands_import.py

def test_strands_agent_import():
    """Strands Agent を import できることを確認"""
    from strands import Agent
    assert Agent is not None

def test_strands_tools_import():
    """Strands Tools を import できることを確認"""
    from strands_agents_tools import Tool
    assert Tool is not None
```

**信頼性レベル**: 🔵

### テストケース 2: 既存テスト非回帰確認

**目的**: 新しい依存の追加が既存テストに影響しないことを確認

```python
# backend/tests/integration/test_existing_functionality.py

def test_bedrock_service_still_works():
    """BedrockService が引き続き動作することを確認"""
    from services.bedrock import BedrockService
    service = BedrockService()
    assert service is not None
    # 既存テスト実行
```

**信頼性レベル**: 🔵

## 信頼性レベルサマリー

| 項目 | 信頼性 | 詳細 |
|------|--------|------|
| requirements.txt 更新 | 🔵 | REQ-SM-001 確定 |
| SAM build 成功 | 🔵 | 標準ビルドプロセス |
| Python import 成功 | 🔵 | 標準検証手段 |
| 既存テスト 260+ 通過 | 🔵 | REQ-SM-405 確定 |
| SDK テスト依存確認 | 🟡 | 要項調査 |

**総合品質**: ✅ **高品質** (80% 以上の信頼性)

## 実装手順

### Step 1: requirements.txt 更新（5分）

1. `backend/requirements.txt` を開く
2. 以下を追加:
   ```
   strands-agents>=0.1.0,<1.0.0
   strands-agents-tools>=0.1.0,<1.0.0
   ```
3. ファイルを保存

### Step 2: 依存解決（10分）

```bash
cd backend
pip install -r requirements.txt
```

- インストール成功を確認
- バージョンコンフリクトがないことを確認

### Step 3: SAM ビルド確認（15分）

```bash
cd backend
sam build
```

- ビルドログを確認
- エラーがないことを確認

### Step 4: インポートテスト実行（5分）

```bash
cd backend
python -c "from strands import Agent; print('Strands SDK import successful')"
```

### Step 5: 既存テスト実行（25分）

```bash
cd backend
make test
```

- 260+ テストが全て通過することを確認
- カバレッジが 80% 以上であることを確認

### Step 6: テストコミット（5分）

新規テストファイル `backend/tests/unit/test_strands_import.py` を追加し、以下を実行:

```bash
git add backend/requirements.txt backend/tests/unit/test_strands_import.py
git commit -m "TASK-0052: Strands Agents SDK 依存追加"
```

## Notes

- Strands Agents SDK は AWS の新しいエージェントフレームワークで、今後の AI 機能拡張の基盤となる
- バージョンピン留めは `>=0.1.0,<1.0.0` の範囲で指定し、マイナーアップデートは自動で取得可能にする
- Lambda Layer ではなく `requirements.txt` での管理により、SAM がビルド時に自動パッケージングする
- 本タスク完了後、TASK-0053 で AIService Protocol インターフェースの実装を開始

## チェックリスト

**実装前の確認**:
- [ ] Strands Agents SDK の最新バージョンを AWS 公式ドキュメントで確認
- [ ] 破壊的変更がないことを確認
- [ ] Python 3.12 互換性を確認

**実装時の確認**:
- [ ] requirements.txt が git tracked されていることを確認
- [ ] SAM build のクリーンビルド（キャッシュなし）を実行
- [ ] 既存テストが全て通過することを確認

**実装後の確認**:
- [ ] `sam build` の成功ログを保存
- [ ] インポートテストの実行ログを保存
- [ ] 既存テスト通過ログ（260+ テスト）を保存
