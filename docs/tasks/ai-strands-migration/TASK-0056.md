# TASK-0056: handler.py AIServiceFactory 統合 + template.yaml 更新

**status**: [x] completed
**phase**: Phase 1 - 基盤構築
**type**: TDD (Red → Green → Refactor)
**hours_estimate**: 8
**hours_actual**: 0
**reliability_index**: 100 (9 🔵 / 0 🟡 / 0 🔴)

## 関連文書

- [ai-strands-migration 概要](../overview.md)
- [要件定義](../../spec/ai-strands-migration/requirements.md)
- [アーキテクチャ設計](../../design/ai-strands-migration/architecture.md)
- [API 仕様](../../design/ai-strands-migration/api-endpoints.md)

## タスク概要

handler.py の BedrockService 直接参照を AIServiceFactory 経由に変更し、template.yaml にフィーチャーフラグ・新環境変数・新 API ルートを追加する。

このタスクは Phase 1 の統合ステップであり、既存 API エンドポイント（generate_cards）を新しい AI サービス機構に適応させ、新しい API ルート（grade-ai, advice）の基盤を準備する。

## 依存タスク

**ブロックの対象**:
- TASK-0057: Strands Agents SDK 統合（grade_answer 実装）
- TASK-0058: Strands Agents SDK 統合（generate_cards 実装）
- TASK-0060: UI エンドポイント実装 + 前処理（advice）
- TASK-0062: Lambda エッジケース処理（generate_cards）

**事前完了が必須**:
- TASK-0055: BedrockAIService Protocol 準拠改修

## 完了条件

- [x] handler.py が AIServiceFactory 経由で AI サービスを取得している
- [x] template.yaml のタイムアウトが 60 秒に更新されている
- [x] USE_STRANDS パラメータが template.yaml に追加されている
- [x] 新環境変数（USE_STRANDS, OLLAMA_HOST, OLLAMA_MODEL）が template.yaml に定義されている
- [x] 新 API ルート（grade-ai, advice）が template.yaml に定義されている
- [x] env.json に新環境変数が追加されている
- [x] 既存テストが全て通過する
- [x] テストカバレッジが 80% 以上である

## 実装詳細

### 1. handler.py の改修

**ファイル**: `backend/src/api/handler.py`

#### import パスの更新

```python
# 旧：
from services.bedrock import BedrockService

# 新：
from services.ai_service import create_ai_service, AIServiceError
```

**信頼性レベル**: 🔵 確定（技術的な標準手順）

#### generate_cards エンドポイントの改修

既存実装例:

```python
# 旧実装：
@app.post("/cards/generate")
async def generate_cards(request_body):
    bedrock_service = BedrockService()
    try:
        result = await bedrock_service.generate_cards(...)
        return {
            "statusCode": 200,
            "body": json.dumps({
                "cards": [card.to_dict() for card in result.cards],
                ...
            })
        }
    except BedrockServiceError as e:
        return {
            "statusCode": 500,
            "body": json.dumps({"error": str(e)})
        }

# 新実装：
@app.post("/cards/generate")
async def generate_cards(request_body):
    ai_service = create_ai_service()
    try:
        result = await ai_service.generate_cards(...)
        return {
            "statusCode": 200,
            "body": json.dumps({
                "cards": [card.to_dict() for card in result.cards],
                ...
            })
        }
    except AIServiceError as e:
        return {
            "statusCode": 500,
            "body": json.dumps({"error": str(e)})
        }
```

**信頼性レベル**: 🔵 確定（既存実装の機能を保持）

#### エラーハンドリング強化

```python
from services.ai_service import (
    AIServiceError,
    AITimeoutError,
    AIRateLimitError,
    AIInternalError,
    AIParseError,
    AIProviderError,
)

def _map_ai_error_to_http(error: AIServiceError) -> dict:
    """AI サービスエラーを HTTP レスポンスにマッピング"""
    if isinstance(error, AITimeoutError):
        return {
            "statusCode": 504,
            "body": json.dumps({"error": "AI service timeout"})
        }
    elif isinstance(error, AIRateLimitError):
        return {
            "statusCode": 429,
            "body": json.dumps({"error": "AI service rate limit exceeded"})
        }
    elif isinstance(error, AIProviderError):
        return {
            "statusCode": 503,
            "body": json.dumps({"error": "AI service unavailable"})
        }
    elif isinstance(error, AIParseError):
        return {
            "statusCode": 500,
            "body": json.dumps({"error": "AI service response parse error"})
        }
    else:
        return {
            "statusCode": 500,
            "body": json.dumps({"error": "AI service error"})
        }
```

**信頼性レベル**: 🔵 確定（API 仕様 api-endpoints.md より）

### 2. template.yaml の更新

**ファイル**: `backend/template.yaml`

#### 2.1 Lambda タイムアウト更新

```yaml
Globals:
  Function:
    Timeout: 60  # 旧値: 30 → 新値: 60（Strands SDK 利用対応）
    MemorySize: 512
    Runtime: python3.12
    Environment:
      Variables:
        LOG_LEVEL: "INFO"
        AWS_LAMBDA_LOG_TYPE: "Tail"
```

**信頼性レベル**: 🔵 確定（Strands SDK 処理時間対応）

#### 2.2 CloudFormation パラメータ追加

```yaml
Parameters:
  UseStrands:
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"
    Description: "Use Strands Agents SDK instead of Bedrock (true/false)"

Conditions:
  ShouldUseStrands: !Equals [!Ref UseStrands, "true"]
```

**信頼性レベル**: 🔵 確定（フィーチャーフラグ設計）

#### 2.3 環境変数定義

```yaml
Globals:
  Function:
    Environment:
      Variables:
        USE_STRANDS: !Ref UseStrands
        OLLAMA_HOST: !If
          - ShouldUseStrands
          - !Sub "http://${OllamaContainerName}:11434"
          - ""
        OLLAMA_MODEL: !If
          - ShouldUseStrands
          - "neural-chat"
          - ""
        # ... 既存環境変数 ...
```

**信頼性レベル**: 🔵 確定（環境変数設計）

#### 2.4 新 API ルート追加

```yaml
Resources:
  MemoruApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: !Ref EnvironmentStage
      Auth:
        DefaultAuthorizer: KeycloakAuthorizer
        # ... 既存設定 ...

  CardsGenerateFunction:
    Type: AWS::Serverless::Function
    Properties:
      # ... 既存設定 ...
      Events:
        GenerateCardsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref MemoruApi
            Path: /cards/generate
            Method: post

  # 新：採点エンドポイント（ハンドラーは後続タスクで実装）
  ReviewsGradeAiFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: api.handler.grade_ai_handler
      Runtime: python3.12
      Timeout: 60
      Environment:
        Variables:
          USE_STRANDS: !Ref UseStrands
          OLLAMA_HOST: !If [ShouldUseStrands, !Sub "http://${OllamaContainerName}:11434", ""]
          OLLAMA_MODEL: !If [ShouldUseStrands, "neural-chat", ""]
      Events:
        GradeAiEvent:
          Type: Api
          Properties:
            RestApiId: !Ref MemoruApi
            Path: /reviews/{card_id}/grade-ai
            Method: post

  # 新：アドバイスエンドポイント（ハンドラーは後続タスクで実装）
  AdviceFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: api.handler.advice_handler
      Runtime: python3.12
      Timeout: 60
      Environment:
        Variables:
          USE_STRANDS: !Ref UseStrands
          OLLAMA_HOST: !If [ShouldUseStrands, !Sub "http://${OllamaContainerName}:11434", ""]
          OLLAMA_MODEL: !If [ShouldUseStrands, "neural-chat", ""]
      Events:
        AdviceEvent:
          Type: Api
          Properties:
            RestApiId: !Ref MemoruApi
            Path: /advice
            Method: get
```

**信頼性レベル**: 🔵 確定（API 仕様）

### 3. env.json の更新

**ファイル**: `backend/env.json`

```json
{
  "MemoruFunction": {
    "USE_STRANDS": "false",
    "OLLAMA_HOST": "http://localhost:11434",
    "OLLAMA_MODEL": "neural-chat"
  },
  "CardsGenerateFunction": {
    "USE_STRANDS": "false",
    "OLLAMA_HOST": "http://localhost:11434",
    "OLLAMA_MODEL": "neural-chat"
  },
  "ReviewsGradeAiFunction": {
    "USE_STRANDS": "false",
    "OLLAMA_HOST": "http://localhost:11434",
    "OLLAMA_MODEL": "neural-chat"
  },
  "AdviceFunction": {
    "USE_STRANDS": "false",
    "OLLAMA_HOST": "http://localhost:11434",
    "OLLAMA_MODEL": "neural-chat"
  }
}
```

**信頼性レベル**: 🔵 確定（ローカル開発環境設定）

## 単体テスト要件

### テストファイル: `backend/tests/unit/test_handler.py`

既存テストを維持しつつ、新規テストを追加:

#### Test 1: AIServiceFactory 統合テスト

```python
from unittest.mock import patch, MagicMock
from services.ai_service import create_ai_service
import pytest

@patch.dict(os.environ, {"USE_STRANDS": "false"})
def test_handler_uses_bedrock_ai_service():
    """handler が USE_STRANDS=false で BedrockAIService を使用すること"""
    from api import handler
    with patch("api.handler.create_ai_service") as mock_factory:
        # ... handler 関数呼び出し ...
        mock_factory.assert_called_once()

@patch.dict(os.environ, {"USE_STRANDS": "true"})
def test_handler_uses_strands_ai_service():
    """handler が USE_STRANDS=true で StrandsAIService を使用すること"""
    from api import handler
    with patch("api.handler.create_ai_service") as mock_factory:
        # ... handler 関数呼び出し ...
        mock_factory.assert_called_once()
```

**信頼性レベル**: 🔵

#### Test 2: 既存 generate_cards エンドポイント互換性

```python
@pytest.mark.asyncio
async def test_generate_cards_endpoint_compatibility():
    """generate_cards エンドポイントが既存動作と互換であること"""
    from api.handler import generate_cards

    request_body = {
        "topic": "History",
        "num_cards": 5,
        "difficulty": "intermediate",
        "language": "ja"
    }

    with patch("api.handler.create_ai_service") as mock_factory:
        mock_service = MagicMock()
        mock_service.generate_cards.return_value = MagicMock(
            cards=[MagicMock(to_dict=lambda: {...})],
            model_used="bedrock",
            processing_time_ms=1000
        )
        mock_factory.return_value = mock_service

        response = await generate_cards(request_body)
        assert response["statusCode"] == 200
        assert "cards" in json.loads(response["body"])
```

**信頼性レベル**: 🔵

#### Test 3: エラーマッピングテスト

```python
@pytest.mark.asyncio
async def test_ai_timeout_error_maps_to_504():
    """AITimeoutError が HTTP 504 にマッピングされること"""
    from api.handler import _map_ai_error_to_http
    from services.ai_service import AITimeoutError

    error = AITimeoutError("Timeout")
    response = _map_ai_error_to_http(error)

    assert response["statusCode"] == 504
    assert "timeout" in json.loads(response["body"])["error"].lower()

@pytest.mark.asyncio
async def test_ai_rate_limit_error_maps_to_429():
    """AIRateLimitError が HTTP 429 にマッピングされること"""
    from api.handler import _map_ai_error_to_http
    from services.ai_service import AIRateLimitError

    error = AIRateLimitError("Rate limit")
    response = _map_ai_error_to_http(error)

    assert response["statusCode"] == 429
    assert "rate limit" in json.loads(response["body"])["error"].lower()

@pytest.mark.asyncio
async def test_ai_provider_error_maps_to_503():
    """AIProviderError が HTTP 503 にマッピングされること"""
    from api.handler import _map_ai_error_to_http
    from services.ai_service import AIProviderError

    error = AIProviderError("Service unavailable")
    response = _map_ai_error_to_http(error)

    assert response["statusCode"] == 503

@pytest.mark.asyncio
async def test_ai_parse_error_maps_to_500():
    """AIParseError が HTTP 500 にマッピングされること"""
    from api.handler import _map_ai_error_to_http
    from services.ai_service import AIParseError

    error = AIParseError("Parse failed")
    response = _map_ai_error_to_http(error)

    assert response["statusCode"] == 500
```

**信頼性レベル**: 🔵

#### Test 4: 環境変数テスト

```python
def test_template_yaml_environment_variables():
    """template.yaml で環境変数が正しく定義されていること"""
    import yaml

    with open("backend/template.yaml", "r") as f:
        template = yaml.safe_load(f)

    env_vars = template["Globals"]["Function"]["Environment"]["Variables"]
    assert "USE_STRANDS" in env_vars
    assert "OLLAMA_HOST" in env_vars
    assert "OLLAMA_MODEL" in env_vars

def test_env_json_contains_new_variables():
    """env.json で新環境変数が定義されていること"""
    import json

    with open("backend/env.json", "r") as f:
        env_config = json.load(f)

    for func_name, vars in env_config.items():
        assert "USE_STRANDS" in vars
        assert "OLLAMA_HOST" in vars
        assert "OLLAMA_MODEL" in vars
```

**信頼性レベル**: 🔵

#### Test 5: API ルート定義テスト

```python
def test_template_yaml_api_routes():
    """template.yaml で新 API ルートが定義されていること"""
    import yaml

    with open("backend/template.yaml", "r") as f:
        template = yaml.safe_load(f)

    resources = template["Resources"]

    # 既存ルート
    assert "CardsGenerateFunction" in resources

    # 新ルート
    assert "ReviewsGradeAiFunction" in resources
    assert "AdviceFunction" in resources

    # ルート定義の確認
    grade_ai_events = resources["ReviewsGradeAiFunction"]["Properties"]["Events"]
    assert "GradeAiEvent" in grade_ai_events
    assert grade_ai_events["GradeAiEvent"]["Properties"]["Path"] == "/reviews/{card_id}/grade-ai"
    assert grade_ai_events["GradeAiEvent"]["Properties"]["Method"] == "post"

    advice_events = resources["AdviceFunction"]["Properties"]["Events"]
    assert "AdviceEvent" in advice_events
    assert advice_events["AdviceEvent"]["Properties"]["Path"] == "/advice"
    assert advice_events["AdviceEvent"]["Properties"]["Method"] == "get"
```

**信頼性レベル**: 🔵

## 信頼性レベルサマリー

| 項目 | 信頼性 | 詳細 |
|------|--------|------|
| handler.py AIServiceFactory 統合 | 🔵 | 設計確定 |
| template.yaml タイムアウト更新 | 🔵 | Strands SDK 対応 |
| CloudFormation パラメータ | 🔵 | フィーチャーフラグ設計 |
| 環境変数定義 | 🔵 | 環境管理設計 |
| 新 API ルート定義 | 🔵 | API 仕様 |
| env.json 更新 | 🔵 | ローカル開発設定 |
| エラーマッピング | 🔵 | HTTP ステータス仕様 |
| 既存エンドポイント互換性 | 🔵 | 後方互換性設計 |
| テストカバレッジ | 🔵 | 単体テスト設計 |

**総合品質**: ✅ **高品質** (100% の信頼性)

## 実装手順

### Step 1: テストケース追加（TDD Red フェーズ）

1. `backend/tests/unit/test_handler.py` を開く
2. 新規テストケースを追加:
   - test_handler_uses_bedrock_ai_service
   - test_handler_uses_strands_ai_service
   - test_generate_cards_endpoint_compatibility
   - test_ai_timeout_error_maps_to_504
   - test_ai_rate_limit_error_maps_to_429
   - test_ai_provider_error_maps_to_503
   - test_ai_parse_error_maps_to_500
   - test_template_yaml_environment_variables
   - test_env_json_contains_new_variables
   - test_template_yaml_api_routes
3. テスト実行: `pytest backend/tests/unit/test_handler.py -v`
4. **期待される結果**: 既存テストは通過、新規テストは失敗（実装未完了）

### Step 2: handler.py 改修（TDD Green フェーズ）

1. `backend/src/api/handler.py` を開く
2. import パスを更新（BedrockService → create_ai_service）
3. generate_cards エンドポイントを改修
4. エラーハンドリング関数 _map_ai_error_to_http を実装
5. 全エンドポイントでエラーマッピングを適用
6. テスト実行: `pytest backend/tests/unit/test_handler.py -v`
7. **期待される結果**: 全テストが通過

### Step 3: template.yaml 更新

1. `backend/template.yaml` を開く
2. Lambda タイムアウトを 60 秒に更新
3. CloudFormation パラメータ UseStrands を追加
4. Condition ShouldUseStrands を追加
5. 環境変数 USE_STRANDS, OLLAMA_HOST, OLLAMA_MODEL を追加
6. 新 Lambda 関数定義を追加:
   - ReviewsGradeAiFunction
   - AdviceFunction
7. SAM ビルド確認: `sam build`
8. **期待される結果**: ビルド成功

### Step 4: env.json 更新

1. `backend/env.json` を開く
2. 全関数に新環境変数を追加:
   - USE_STRANDS
   - OLLAMA_HOST
   - OLLAMA_MODEL

### Step 5: リファクタリング（TDD Refactor フェーズ）

1. エラーメッセージを改善
2. ドキュメント文字列を追加
3. ログレベルを設定
4. テスト実行: `pytest backend/tests/unit/test_handler.py -v`
5. **期待される結果**: 全テストが通過・カバレッジ 80% 以上

### Step 6: 既存テスト確認

```bash
cd backend
make test
```

- **期待される結果**: 260+ テストが全て通過

### Step 7: SAM ローカルテスト

```bash
cd backend
sam build
sam local start-api --env-vars env.json
```

- **期待される結果**: API が正常に起動、既存エンドポイントが動作

### Step 8: コミット

```bash
git add backend/src/api/handler.py backend/template.yaml backend/env.json backend/tests/unit/test_handler.py
git commit -m "TASK-0056: handler.py AIServiceFactory 統合 + template.yaml 更新"
```

## Notes

- 新 API ルート（grade-ai, advice）はこのタスクでは定義のみ、ハンドラー実装はなし
- ハンドラー実装は後続タスク（TASK-0060, TASK-0062）で実施
- USE_STRANDS パラメータのデフォルト値は "false"（既存動作を保持）
- env.json のローカル開発値も "false" に設定
- CloudFormation Condition を使用して、USE_STRANDS=true の場合のみ環境変数を設定
- 既存エンドポイントは USE_STRANDS フラグに関わらず動作を継続

## チェックリスト

**実装前**:
- [ ] 既存 handler.py の全エンドポイントを確認
- [ ] 既存テスト test_handler.py の全テストケースを確認
- [ ] API 仕様 api-endpoints.md を確認
- [ ] 既存 template.yaml の構造を確認

**実装時**:
- [ ] import パスが正しく更新されたこと
- [ ] エラーマッピング関数が全例外タイプをカバーしていること
- [ ] template.yaml の YAML 構文が正しいこと
- [ ] env.json の JSON 構文が正しいこと
- [ ] 新 Lambda 関数の定義が正しいこと

**実装後**:
- [ ] 既存テストが全て通過すること
- [ ] 新規テストが全て通過すること
- [ ] テストカバレッジが 80% 以上であること
- [ ] SAM ビルドが成功すること
- [ ] SAM ローカルテストが成功すること
- [ ] 既存テスト 260+ が全て通過すること
- [ ] 既存エンドポイントの動作が変わらないこと
