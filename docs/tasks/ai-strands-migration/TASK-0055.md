# TASK-0055: BedrockAIService Protocol 準拠改修

**status**: [x] completed
**phase**: Phase 1 - 基盤構築
**type**: TDD (Red → Green → Refactor)
**hours_estimate**: 8
**hours_actual**: 0
**reliability_index**: 100 (12 🔵 / 0 🟡 / 0 🔴)

## 関連文書

- [ai-strands-migration 概要](../overview.md)
- [要件定義](../../spec/ai-strands-migration/requirements.md)
- [アーキテクチャ設計](../../design/ai-strands-migration/architecture.md)
- [インターフェース定義](../../design/ai-strands-migration/interfaces.py)

## タスク概要

既存の BedrockService を AIService Protocol に準拠するよう改修する。例外クラスを AIServiceError 階層にマッピングし、generate_cards() の既存実装を維持しつつ、grade_answer() と get_learning_advice() のスタブ実装を追加する。

このタスクは Phase 1 の核となる部分であり、既存の Bedrock 統合コードを新しい Protocol インターフェースに適応させる。

## 依存タスク

**ブロックの対象**:
- TASK-0056: handler.py AIServiceFactory 統合 + template.yaml 更新
- TASK-0058: Strands Agents SDK 統合（generate_cards 実装）

**事前完了が必須**:
- TASK-0053: AIService Protocol + 共通型定義 + 例外階層
- TASK-0054: プロンプトモジュールディレクトリ化

## 完了条件

- [x] BedrockService が AIService Protocol の全メソッドを実装している
- [x] 例外クラスが AIServiceError 階層に統合されている
- [x] generate_cards() の既存実装が変更なく動作している
- [x] grade_answer() が Bedrock API を呼び出して GradingResult を返す
- [x] get_learning_advice() が Bedrock API を呼び出して LearningAdvice を返す
- [x] 既存テスト（test_bedrock.py）が全て通過する
- [x] テストカバレッジが 80% 以上である

## 実装詳細

### 1. BedrockService クラス改修

**ファイル**: `backend/src/services/bedrock.py`

#### クラス定義と Protocol 準拠

```python
from typing import Optional
from services.ai_service import (
    AIService,
    AIServiceError,
    AITimeoutError,
    AIRateLimitError,
    AIInternalError,
    AIParseError,
    GeneratedCard,
    GenerationResult,
    GradingResult,
    LearningAdvice,
    ReviewSummary,
    DifficultyLevel,
    Language,
)
from services.prompts import (
    get_card_generation_prompt,
    get_grading_prompt,
    get_advice_prompt,
)

class BedrockService:
    """Bedrock API を使用した AI サービス実装"""

    def __init__(self):
        """Bedrock クライアントを初期化"""
        import boto3
        self.bedrock_runtime = boto3.client("bedrock-runtime", region_name="ap-northeast-1")
        self.model_id = "anthropic.claude-3-haiku-20240307-v1:0"

    async def generate_cards(
        self,
        topic: str,
        num_cards: int = 5,
        difficulty: DifficultyLevel = "intermediate",
        language: Language = "ja"
    ) -> GenerationResult:
        """
        暗記カードを生成する（既存実装）

        Args:
            topic: カードのトピック
            num_cards: 生成カード数（デフォルト 5）
            difficulty: 難易度（"easy", "intermediate", "hard"）
            language: 言語（"ja", "en"）

        Returns:
            GenerationResult: 生成されたカード情報

        Raises:
            AITimeoutError: リクエストタイムアウト
            AIRateLimitError: レート制限
            AIInternalError: Bedrock API エラー
            AIParseError: レスポンス解析エラー
        """
        # 既存実装を維持
        # ... 既存のコード ...
        pass
```

**信頼性レベル**: 🔵 確定（既存実装の維持が確定）

#### 2. grade_answer() メソッド実装

```python
    async def grade_answer(
        self,
        card_front: str,
        card_back: str,
        user_answer: str,
        language: Language = "ja"
    ) -> GradingResult:
        """
        ユーザーの回答を採点する（0-5）

        Args:
            card_front: カード表（質問）
            card_back: カード裏（正解）
            user_answer: ユーザー回答
            language: 言語（"ja", "en"）

        Returns:
            GradingResult: 採点結果（grade 0-5、reasoning、processing_time_ms）

        Raises:
            AITimeoutError: リクエストタイムアウト
            AIRateLimitError: レート制限
            AIInternalError: Bedrock API エラー
            AIParseError: レスポンス解析エラー
        """
        import json
        import time
        from datetime import datetime

        try:
            start_time = time.time()

            prompt = get_grading_prompt(
                card_front=card_front,
                card_back=card_back,
                user_answer=user_answer,
                language=language
            )

            # Bedrock API 呼び出し
            response = self.bedrock_runtime.invoke_model(
                modelId=self.model_id,
                contentType="application/json",
                accept="application/json",
                body=json.dumps({
                    "anthropic_version": "bedrock-2023-06-01",
                    "max_tokens": 512,
                    "messages": [
                        {
                            "role": "user",
                            "content": prompt
                        }
                    ]
                })
            )

            # レスポンス解析
            response_body = json.loads(response["body"].read())
            content = response_body["content"][0]["text"]
            result_json = json.loads(content)

            processing_time_ms = (time.time() - start_time) * 1000

            return GradingResult(
                grade=int(result_json.get("grade", 2)),
                reasoning=result_json.get("reasoning", ""),
                feedback=result_json.get("feedback", ""),
                model_used="bedrock",
                processing_time_ms=processing_time_ms
            )

        except json.JSONDecodeError as e:
            raise AIParseError(f"Failed to parse Bedrock response: {e}")
        except KeyError as e:
            raise AIParseError(f"Missing required field in response: {e}")
        except Exception as e:
            if "TimeoutError" in str(type(e)):
                raise AITimeoutError(f"Bedrock request timed out: {e}")
            elif "ThrottlingException" in str(e):
                raise AIRateLimitError(f"Bedrock rate limit exceeded: {e}")
            else:
                raise AIInternalError(f"Bedrock API error: {e}")
```

**信頼性レベル**: 🔵 確定（Bedrock API 標準実装）

#### 3. get_learning_advice() メソッド実装

```python
    async def get_learning_advice(
        self,
        review_summary: ReviewSummary,
        language: Language = "ja"
    ) -> LearningAdvice:
        """
        学習アドバイスを生成する

        Args:
            review_summary: 復習統計サマリー
            language: 言語（"ja", "en"）

        Returns:
            LearningAdvice: 学習アドバイス情報

        Raises:
            AITimeoutError: リクエストタイムアウト
            AIRateLimitError: レート制限
            AIInternalError: Bedrock API エラー
            AIParseError: レスポンス解析エラー
        """
        import json
        import time

        try:
            start_time = time.time()

            prompt = get_advice_prompt(
                review_summary=review_summary,
                language=language
            )

            # Bedrock API 呼び出し
            response = self.bedrock_runtime.invoke_model(
                modelId=self.model_id,
                contentType="application/json",
                accept="application/json",
                body=json.dumps({
                    "anthropic_version": "bedrock-2023-06-01",
                    "max_tokens": 1024,
                    "messages": [
                        {
                            "role": "user",
                            "content": prompt
                        }
                    ]
                })
            )

            # レスポンス解析
            response_body = json.loads(response["body"].read())
            content = response_body["content"][0]["text"]
            result_json = json.loads(content)

            processing_time_ms = (time.time() - start_time) * 1000

            return LearningAdvice(
                advice_text=result_json.get("advice_text", ""),
                weak_areas=result_json.get("weak_areas", []),
                recommendations=result_json.get("recommendations", []),
                model_used="bedrock",
                processing_time_ms=processing_time_ms
            )

        except json.JSONDecodeError as e:
            raise AIParseError(f"Failed to parse Bedrock response: {e}")
        except KeyError as e:
            raise AIParseError(f"Missing required field in response: {e}")
        except Exception as e:
            if "TimeoutError" in str(type(e)):
                raise AITimeoutError(f"Bedrock request timed out: {e}")
            elif "ThrottlingException" in str(e):
                raise AIRateLimitError(f"Bedrock rate limit exceeded: {e}")
            else:
                raise AIInternalError(f"Bedrock API error: {e}")
```

**信頼性レベル**: 🔵 確定（Bedrock API 標準実装）

### 2. 例外クラス統合

既存の例外クラスを AIServiceError 階層に統合:

```python
from services.ai_service import (
    AIServiceError,
    AITimeoutError as AITimeoutErrorBase,
    AIRateLimitError as AIRateLimitErrorBase,
    AIInternalError as AIInternalErrorBase,
    AIParseError as AIParseErrorBase,
)

# 既存例外を新しい階層に統合
class BedrockServiceError(AIServiceError):
    """Bedrock サービスエラー（基底）"""
    pass

class BedrockTimeoutError(AITimeoutErrorBase, BedrockServiceError):
    """Bedrock タイムアウトエラー"""
    pass

class BedrockRateLimitError(AIRateLimitErrorBase, BedrockServiceError):
    """Bedrock レート制限エラー"""
    pass

class BedrockInternalError(AIInternalErrorBase, BedrockServiceError):
    """Bedrock 内部エラー"""
    pass

class BedrockParseError(AIParseErrorBase, BedrockServiceError):
    """Bedrock レスポンス解析エラー"""
    pass
```

**信頼性レベル**: 🔵 確定（後方互換性を保ちながら統合）

### 3. 例外マッピングの実装

上記の grade_answer() と get_learning_advice() の実装に含まれる。

**信頼性レベル**: 🔵 確定（技術的な標準手順）

### 4. 既存実装の互換性

- BedrockService の既存メソッド（generate_cards など）は変更なし
- 既存の BedrockServiceError を catch しているコードも動作
- 新しいコードは AIServiceError を catch 可能

**信頼性レベル**: 🔵 確定（後方互換性設計）

## 単体テスト要件

### テストファイル: `backend/tests/unit/test_bedrock.py`

既存テストを維持しつつ、新規メソッド用テストを追加:

#### Test 1: 既存実装の非回帰確認

```python
@pytest.mark.asyncio
async def test_generate_cards_compatibility():
    """generate_cards の既存実装が変更されていないことを確認"""
    service = BedrockService()
    result = await service.generate_cards(
        topic="History",
        num_cards=3,
        difficulty="easy",
        language="en"
    )
    assert isinstance(result, GenerationResult)
    assert len(result.cards) > 0
    assert result.model_used == "bedrock"
```

**信頼性レベル**: 🔵

#### Test 2: grade_answer メソッドテスト

```python
@pytest.mark.asyncio
async def test_grade_answer_success():
    """grade_answer が正常に動作すること"""
    service = BedrockService()
    result = await service.grade_answer(
        card_front="What is Python?",
        card_back="A programming language",
        user_answer="A programming language",
        language="en"
    )
    assert isinstance(result, GradingResult)
    assert 0 <= result.grade <= 5
    assert isinstance(result.reasoning, str)
    assert result.model_used == "bedrock"
    assert result.processing_time_ms > 0

@pytest.mark.asyncio
async def test_grade_answer_wrong_answer():
    """間違った回答が低いグレードで採点されること"""
    service = BedrockService()
    result = await service.grade_answer(
        card_front="What is Python?",
        card_back="A programming language",
        user_answer="A type of snake",
        language="en"
    )
    assert isinstance(result, GradingResult)
    assert 0 <= result.grade <= 5

@pytest.mark.asyncio
async def test_grade_answer_japanese():
    """日本語での採点テスト"""
    service = BedrockService()
    result = await service.grade_answer(
        card_front="Pythonとは何か？",
        card_back="プログラミング言語",
        user_answer="プログラミング言語",
        language="ja"
    )
    assert isinstance(result, GradingResult)
    assert 0 <= result.grade <= 5
```

**信頼性レベル**: 🔵

#### Test 3: get_learning_advice メソッドテスト

```python
@pytest.mark.asyncio
async def test_get_learning_advice_success():
    """get_learning_advice が正常に動作すること"""
    service = BedrockService()
    summary = ReviewSummary(
        total_reviews=100,
        average_grade=3.2,
        total_cards=50,
        cards_due_today=10,
        streak_days=5,
        tag_performance={"noun": 3.8, "verb": 2.8},
        recent_review_dates=[datetime.now()]
    )
    result = await service.get_learning_advice(
        review_summary=summary,
        language="en"
    )
    assert isinstance(result, LearningAdvice)
    assert isinstance(result.advice_text, str)
    assert isinstance(result.weak_areas, list)
    assert isinstance(result.recommendations, list)
    assert result.model_used == "bedrock"
    assert result.processing_time_ms > 0

@pytest.mark.asyncio
async def test_get_learning_advice_with_low_score():
    """低いスコアの場合のアドバイステスト"""
    service = BedrockService()
    summary = ReviewSummary(
        total_reviews=50,
        average_grade=2.0,  # 低い
        total_cards=25,
        cards_due_today=8,
        streak_days=2,
        tag_performance={"weak": 1.5},
        recent_review_dates=[datetime.now()]
    )
    result = await service.get_learning_advice(
        review_summary=summary,
        language="en"
    )
    assert isinstance(result, LearningAdvice)
    # 弱点分野がアドバイスに含まれると期待
```

**信頼性レベル**: 🔵

#### Test 4: 例外ハンドリングテスト

```python
@pytest.mark.asyncio
async def test_grade_answer_api_error():
    """API エラー時に AIInternalError が発生すること"""
    service = BedrockService()
    with patch.object(service.bedrock_runtime, "invoke_model") as mock:
        mock.side_effect = Exception("Service error")
        with pytest.raises(AIInternalError):
            await service.grade_answer(
                card_front="Q",
                card_back="A",
                user_answer="A"
            )

@pytest.mark.asyncio
async def test_grade_answer_parse_error():
    """レスポンス解析エラー時に AIParseError が発生すること"""
    service = BedrockService()
    with patch.object(service.bedrock_runtime, "invoke_model") as mock:
        mock.return_value = {"body": MockStream('invalid json')}
        with pytest.raises(AIParseError):
            await service.grade_answer(
                card_front="Q",
                card_back="A",
                user_answer="A"
            )

@pytest.mark.asyncio
async def test_exception_hierarchy():
    """例外が AIServiceError を継承していること"""
    assert issubclass(BedrockServiceError, AIServiceError)
    assert issubclass(BedrockTimeoutError, AIServiceError)
    assert issubclass(BedrockRateLimitError, AIServiceError)
    assert issubclass(BedrockInternalError, AIServiceError)
    assert issubclass(BedrockParseError, AIServiceError)
```

**信頼性レベル**: 🔵

## 信頼性レベルサマリー

| 項目 | 信頼性 | 詳細 |
|------|--------|------|
| BedrockService AIService Protocol | 🔵 | インターフェース設計確定 |
| generate_cards 既存実装維持 | 🔵 | 確定（変更なし） |
| grade_answer 実装 | 🔵 | Bedrock API 標準実装 |
| get_learning_advice 実装 | 🔵 | Bedrock API 標準実装 |
| 例外クラス統合 | 🔵 | 後方互換性設計 |
| 例外マッピング | 🔵 | 技術的標準手順 |
| テスト非回帰確認 | 🔵 | 既存テスト 260+ |
| 新規メソッドテスト | 🔵 | 単体テスト設計 |
| import パス更新 | 🔵 | 技術的標準手順 |
| Protocol 準拠検証 | 🔵 | Runtime チェック |

**総合品質**: ✅ **高品質** (100% の信頼性)

## 実装手順

### Step 1: テストケース追加（TDD Red フェーズ）

1. `backend/tests/unit/test_bedrock.py` を開く
2. 既存テストを確認
3. 新規テストケースを追加:
   - test_grade_answer_success
   - test_grade_answer_wrong_answer
   - test_grade_answer_japanese
   - test_get_learning_advice_success
   - test_get_learning_advice_with_low_score
   - test_grade_answer_api_error
   - test_grade_answer_parse_error
   - test_exception_hierarchy
4. テスト実行: `pytest backend/tests/unit/test_bedrock.py -v`
5. **期待される結果**: 既存テストは通過、新規テストは失敗（メソッド未実装）

### Step 2: BedrockService 改修（TDD Green フェーズ）

1. `backend/src/services/bedrock.py` を開く
2. import パスを更新（ai_service, prompts から新規クラスをインポート）
3. 例外クラスを統合（AIServiceError 階層に合わせる）
4. grade_answer() メソッドを実装
5. get_learning_advice() メソッドを実装
6. generate_cards() は既存実装を維持
7. テスト実行: `pytest backend/tests/unit/test_bedrock.py -v`
8. **期待される結果**: 全テストが通過

### Step 3: 例外ハンドリング強化（TDD Refactor フェーズ）

1. エラーメッセージを改善
2. 例外マッピングを検証
3. ログレベルを設定
4. テスト実行: `pytest backend/tests/unit/test_bedrock.py -v -s`
5. **期待される結果**: 全テストが通過・カバレッジ 80% 以上

### Step 4: 既存テスト確認

```bash
cd backend
make test
```

- **期待される結果**: 260+ テストが全て通過

### Step 5: コミット

```bash
git add backend/src/services/bedrock.py backend/tests/unit/test_bedrock.py
git commit -m "TASK-0055: BedrockAIService Protocol 準拠改修"
```

## Notes

- 既存の BedrockService クラス名を変更しない（後方互換性）
- BedrockServiceError は AIServiceError の両方を継承する多重継承を使用
- 既存コードで `BedrockServiceError` を catch しているコードも引き続き動作
- 新しいコードは `AIServiceError` を catch 可能
- grade_answer() と get_learning_advice() は async メソッド（Bedrock API 呼び出し）
- 処理時間計測は milliseconds で行う

## チェックリスト

**実装前**:
- [ ] 既存 bedrock.py の全メソッドを確認
- [ ] 既存テスト test_bedrock.py の全テストケースを確認
- [ ] AIService Protocol の全メソッドシグネチャを確認

**実装時**:
- [ ] import パスが正しく更新されたこと
- [ ] 例外クラスが多重継承で定義されていること
- [ ] grade_answer() が GradingResult を返すこと
- [ ] get_learning_advice() が LearningAdvice を返すこと
- [ ] 例外マッピングが正しいこと

**実装後**:
- [ ] 既存テストが全て通過すること
- [ ] 新規テストが全て通過すること
- [ ] テストカバレッジが 80% 以上であること
- [ ] 既存テスト 260+ が全て通過すること
- [ ] 後方互換性が保たれていること
