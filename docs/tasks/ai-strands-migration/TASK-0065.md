# TASK-0065: 全体統合テスト + 品質確認

**status**: [ ] pending
**phase**: Phase 4 - ローカル開発 + 最終検証
**type**: TDD
**hours_estimate**: 6
**hours_actual**: 0
**reliability_index**: 100 (9 🔵 / 0 🟡 / 0 🔴)

## 関連文書

- [ai-strands-migration 概要](../overview.md)
- [要件定義](../../spec/ai-strands-migration/requirements.md)
- [アーキテクチャ設計](../../design/ai-strands-migration/architecture.md)
- [API エンドポイント](../../design/ai-strands-migration/api-endpoints.md)
- [インターフェース定義](../../design/ai-strands-migration/interfaces.py)

## タスク概要

AI Strands Migration の全実装に対する最終品質確認。全テスト実行、テストカバレッジ確認、既存テスト保護、全エンドポイントの動作検証を実施する。

このタスクは Phase 1～4 の全実装のまとめであり、マイグレーション完了の最終ゲートキーパーとなる。

## 依存タスク

**前提条件**:
- TASK-0063: DynamoDB Transaction 層実装
- TASK-0064: Ollama プロバイダー + docker-compose 統合

**ブロックの対象**: None（最終タスク）

## 完了条件

- [ ] 全テストが通過する（既存 260+ テスト + 新規テスト）
- [ ] テストカバレッジが 80% 以上
- [ ] 全3エンドポイント（/grade, /explain, /suggest）が USE_STRANDS=true/false で動作する
- [ ] コードレビュー観点（Protocol 準拠性、エラーハンドリング、ログ、セキュリティ）の確認が完了している
- [ ] CLAUDE.md の進捗セクションが更新されている
- [ ] overview.md のタスク一覧が更新されている

## 実装詳細

### 1. 全テスト実行

#### テスト実行コマンド

```bash
cd backend
make test
```

**期待される出力**:
- 全テストが通過（✓ または PASS で表示）
- テストカバレッジ報告（80% 以上）
- 実行時間（通常 2～5分）

**信頼性レベル**: 🔵 確定（REQ-SM-404/405 より）
- 既存テストスイート 260+ は全て保護される
- 新規テストは Phase 1-3 で追加済み

#### 既存テスト保護の確認

既存テスト数を確認:

```bash
cd backend
find tests/ -name "test_*.py" -o -name "*_test.py" | xargs wc -l | tail -1
# 出力例: 12000 total （260+ テストケース）
```

**信頼性レベル**: 🔵 確定

### 2. テストカバレッジ分析

#### カバレッジ報告の取得

```bash
cd backend
pytest --cov=src --cov-report=term-missing --cov-report=html
```

**期待される結果**:
- 全体カバレッジ 80% 以上
- `src/services/ai_service.py`: 85% 以上
- `src/services/strands_service.py`: 85% 以上
- `src/services/bedrock.py`: 80% 以上
- `src/handlers/`: 80% 以上

**信頼性レベル**: 🔵 確定

#### カバレッジ分析レポート

`htmlcov/index.html` を開いて以下を確認:

1. **AIService Protocol（src/types/ai_service.py）**:
   - Protocol 定義: 100% （型定義のため必須）
   - 実装クラス（StrandsAIService, BedrockService）: 85% 以上

2. **Strands Service（src/services/strands_service.py）**:
   - `_create_model()`: 85% 以上
   - `_create_graph()`: 85% 以上
   - `process()`: 85% 以上
   - エラーハンドリングパス: 80% 以上

3. **Bedrock Service（src/services/bedrock.py）**:
   - `process()`: 80% 以上
   - プロンプト構築: 80% 以上

4. **Handlers（src/handlers/）**:
   - `/grade` エンドポイント: 80% 以上
   - `/explain` エンドポイント: 80% 以上
   - `/suggest` エンドポイント: 80% 以上
   - エラーハンドリング: 80% 以上

5. **Prompts（src/prompts/）**:
   - 全プロンプトテンプレート: 75% 以上

**信頼性レベル**: 🔵 確定

### 3. コードレビュー観点の確認

#### 3.1 Protocol 準拠性（型安全性）

```bash
cd backend
mypy src/ --strict
```

**確認項目**:
- [ ] AIService Protocol の全メソッド署名が実装クラスで満たされている
- [ ] Optional[T] 型が正しく処理されている
- [ ] 型キャストが不要な設計になっている
- [ ] Union 型の分岐が網羅的である

**信頼性レベル**: 🔵 確定

#### 3.2 エラーハンドリングの統一性

以下のファイルでエラーハンドリングを確認:

```python
# backend/src/services/strands_service.py, bedrock.py, handlers/ などで確認

# パターン 1: AIServiceError 基底クラスの使用
try:
    result = service.process(request)
except AIServiceError as e:
    # 統一されたエラーハンドリング
    logger.error(f"AI Service Error: {e}", extra={"error_code": e.error_code})
    return error_response(e)

# パターン 2: カスタム例外の定義
class PromptValidationError(AIServiceError):
    """プロンプト検証エラー"""
    error_code = "PROMPT_VALIDATION_ERROR"

# パターン 3: リトライ可能なエラー
class TemporaryServiceError(AIServiceError):
    """一時的なサービスエラー（リトライ推奨）"""
    is_retryable = True
```

**確認項目**:
- [ ] 全ての例外が AIServiceError または既知の例外を継承している
- [ ] エラーログに `error_code` が含まれている
- [ ] HTTP ステータスコード（4xx, 5xx）が適切に割り当てられている
- [ ] ユーザー向けメッセージが機密情報を含まない
- [ ] リトライ可能なエラーが識別可能である

**信頼性レベル**: 🔵 確定

#### 3.3 ログ出力の適切性

```bash
cd backend
grep -r "logger\." src/ | wc -l
# 出力例: 150+ ログステートメント
```

**確認項目**:
- [ ] INFO レベル: ユーザー向けの重要な操作（grade 実行、モデル選択等）
- [ ] DEBUG レベル: 開発者向けの詳細情報（プロンプト内容、レスポンス解析）
- [ ] WARNING レベル: 潜在的な問題（フォールバック、リトライ）
- [ ] ERROR レベル: エラー発生時の詳細（例外スタック、エラーコード）

**ログ出力例**:

```python
# 良い例
logger.info(
    "Grade card",
    extra={
        "card_id": card_id,
        "service": "strands",
        "status": "started"
    }
)

logger.debug(
    "Ollama request",
    extra={
        "prompt_tokens": prompt_tokens,
        "model": "llama3.2"
    }
)

logger.error(
    "Grade process failed",
    extra={
        "card_id": card_id,
        "error_code": "GRADE_FAILED",
        "reason": str(e)
    },
    exc_info=True
)
```

**信頼性レベル**: 🔵 確定

#### 3.4 セキュリティ（プロンプトインジェクション対策）

**確認項目**:

1. **ユーザー入力の検証**:
   ```python
   # ✓ 良い例
   def validate_user_input(text: str) -> None:
       if len(text) > MAX_LENGTH:
           raise InputValidationError("Text too long")
       if contains_prompt_injection_patterns(text):
           raise InputValidationError("Suspicious input detected")
   ```

2. **プロンプトテンプレートの分離**:
   ```python
   # ✓ 良い例（プロンプト定数と入力を分離）
   GRADE_PROMPT = """
   You are a Japanese language learning tutor.
   Card content: {card_content}
   User answer: {user_answer}
   Your evaluation:
   """

   # 埋め込みは安全に実施
   prompt = GRADE_PROMPT.format(
       card_content=sanitize(card_content),
       user_answer=sanitize(user_answer)
   )
   ```

3. **システムプロンプトの保護**:
   - [ ] システムプロンプトがユーザー入力を含まない
   - [ ] モデルの指示がオーバーライドされない設計

**信頼性レベル**: 🔵 確定

### 4. 全エンドポイントの動作検証

#### 4.1 /grade エンドポイント

**テスト方法 1: USE_STRANDS=true（Ollama）**:

```bash
cd backend

# 1. サービス起動
make local-all
make local-api  # 別ターミナル

# 2. テスト実行
export USE_STRANDS=true
curl -X POST http://localhost:8080/grade \
  -H "Content-Type: application/json" \
  -d '{
    "card_id": "test-card-001",
    "card_content": "日本語の「こんにちは」の意味は？",
    "user_answer": "hello in Japanese"
  }'

# 期待される応答:
# { "grade": "correct", "feedback": "..." }
```

**テスト方法 2: USE_STRANDS=false（Bedrock）**:

```bash
export USE_STRANDS=false
# 同上のテストを実行
# Bedrock 認証情報が必要（AWS credentials）
```

**確認項目**:
- [ ] HTTP 200 で応答
- [ ] `grade` フィールドが "correct", "partial", "incorrect" のいずれか
- [ ] `feedback` フィールドが日本語で返される
- [ ] `explanation` フィールドが存在する
- [ ] `confidence_score` フィールドが 0.0～1.0 の範囲
- [ ] ログに card_id が記録される

**信頼性レベル**: 🔵 確定

#### 4.2 /explain エンドポイント

```bash
# USE_STRANDS=true
curl -X POST http://localhost:8080/explain \
  -H "Content-Type: application/json" \
  -d '{
    "card_id": "test-card-002",
    "card_content": "日本の首都は？"
  }'

# 期待される応答:
# { "explanation": "...", "related_concepts": [...] }
```

**確認項目**:
- [ ] HTTP 200 で応答
- [ ] `explanation` フィールドが存在し、日本語である
- [ ] `related_concepts` 配列が存在
- [ ] リレーテッドコンセプトが 3～5 個含まれている

**信頼性レベル**: 🔵 確定

#### 4.3 /suggest エンドポイント

```bash
# USE_STRANDS=true
curl -X POST http://localhost:8080/suggest \
  -H "Content-Type: application/json" \
  -d '{
    "card_id": "test-card-003",
    "learning_history": ["correct", "incorrect", "correct"]
  }'

# 期待される応答:
# { "suggestion": "...", "next_review_interval": 7 }
```

**確認項目**:
- [ ] HTTP 200 で応答
- [ ] `suggestion` フィールドが存在し、日本語である
- [ ] `next_review_interval` が正の整数（日数）
- [ ] 学習履歴が考慮されている

**信頼性レベル**: 🔵 確定

### 5. ドキュメント更新

#### 5.1 CLAUDE.md の進捗セクション更新

`/Volumes/external/dev/memoru-liff/CLAUDE.md` の以下セクションを更新:

```markdown
#### ai-strands-migration（AI Strands SDK 移行）
- [x] Phase 1: 基盤構築 (TASK-0052 ~ TASK-0054)
- [x] Phase 2: Strands SDK 統合実装 (TASK-0055 ~ TASK-0059)
- [x] Phase 3: エラーハンドリング + アーキテクチャ強化 (TASK-0060 ~ TASK-0063)
- [x] Phase 4: ローカル開発 + 最終検証 (TASK-0064 ~ TASK-0065)

**最終状態**: ✅ 完了 - Bedrock から Strands Agents SDK へのマイグレーション完了
```

**信頼性レベル**: 🔵 確定

#### 5.2 overview.md のタスク一覧更新

`docs/tasks/ai-strands-migration/overview.md` のタスク状態を更新:

```markdown
| Task ID | 要件コード | タスク名 | 状態 | Hours |
|---------|-----------|---------|------|-------|
| TASK-0052 | REQ-SM-001 | Strands Agents SDK 依存追加 + ビルド確認 | [x] | 4 |
| TASK-0053 | REQ-SM-002/003 | AIService Protocol + 共通型定義 + 例外階層 | [x] | 8 |
| ... | ... | ... | [x] | ... |
| TASK-0064 | REQ-SM-101/005 | Ollama プロバイダー + docker-compose 統合 | [x] | 6 |
| TASK-0065 | REQ-SM-404/405 | 全体統合テスト + 品質確認 | [x] | 6 |

**Phase 完了条件**:
- [x] 全テスト通過（260+ テスト）
- [x] テストカバレッジ 80% 以上
- [x] 全エンドポイント動作確認
- [x] コードレビュー観点確認完了
```

**信頼性レベル**: 🔵 確定

## 単体テスト要件

### テストケース 1: 全テスト実行と通過確認

**目的**: 全テストスイート（既存 260+ + 新規）が正常に動作することを確認

```bash
# backend/tests/ で実行
cd backend
pytest -v --tb=short 2>&1 | tee test-results.log

# 検証項目:
# - テスト数: 260+ 件以上
# - 失敗数: 0 件
# - スキップ数: 0 件（許可されたものを除く）
```

**信頼性レベル**: 🔵

### テストケース 2: テストカバレッジ確認

**目的**: テストカバレッジが 80% 以上であることを確認

```bash
cd backend
pytest --cov=src --cov-report=term-missing --cov-report=html

# 検証項目:
# - 全体カバレッジ: 80% 以上
# - ai_service.py: 85% 以上
# - strands_service.py: 85% 以上
# - bedrock.py: 80% 以上
# - handlers/: 80% 以上
```

**信頼性レベル**: 🔵

### テストケース 3: Protocol 準拠性テスト

**目的**: 全実装クラスが AIService Protocol を満たしていることを確認

```python
# backend/tests/unit/test_protocol_compliance.py

from typing import get_type_hints
from src.types.ai_service import AIService
from src.services.strands_service import StrandsAIService
from src.services.bedrock import BedrockService

def test_strands_ai_service_implements_protocol():
    """StrandsAIService が AIService Protocol を実装していることを確認"""
    protocol_methods = {
        "process",
        "validate_request",
        "parse_response"
    }
    service_methods = {
        method for method in dir(StrandsAIService)
        if not method.startswith("_")
    }
    assert protocol_methods.issubset(service_methods), \
        f"Missing methods: {protocol_methods - service_methods}"

def test_bedrock_service_implements_protocol():
    """BedrockService が AIService Protocol を実装していることを確認"""
    # 上記と同様
```

**信頼性レベル**: 🔵

### テストケース 4: 全エンドポイント動作テスト

**目的**: 全 3 エンドポイントが正常に動作することを確認

```python
# backend/tests/integration/test_all_endpoints.py

import json
from src.handlers.grade import lambda_handler as grade_handler
from src.handlers.explain import lambda_handler as explain_handler
from src.handlers.suggest import lambda_handler as suggest_handler

def test_grade_endpoint_with_strands():
    """grade エンドポイントが Strands で動作することを確認"""
    os.environ["USE_STRANDS"] = "true"
    event = {
        "body": json.dumps({
            "card_id": "test-001",
            "card_content": "テスト",
            "user_answer": "answer"
        })
    }
    response = grade_handler(event, None)
    assert response["statusCode"] == 200
    assert "grade" in json.loads(response["body"])

def test_grade_endpoint_with_bedrock():
    """grade エンドポイントが Bedrock で動作することを確認"""
    os.environ["USE_STRANDS"] = "false"
    # 上記と同様（AWS credentials 必要）

def test_explain_endpoint():
    """explain エンドポイントが動作することを確認"""
    # 同様のテスト

def test_suggest_endpoint():
    """suggest エンドポイントが動作することを確認"""
    # 同様のテスト
```

**信頼性レベル**: 🔵

### テストケース 5: エラーハンドリング統一性テスト

**目的**: エラーハンドリングが統一されていることを確認

```python
# backend/tests/unit/test_error_handling.py

from src.exceptions import AIServiceError, PromptValidationError
from src.services.strands_service import StrandsAIService

def test_ai_service_error_hierarchy():
    """AIServiceError の階層が正しいことを確認"""
    assert issubclass(PromptValidationError, AIServiceError)
    assert issubclass(GradeFailedError, AIServiceError)

def test_error_logging_includes_error_code():
    """エラーログに error_code が含まれていることを確認"""
    with patch("src.logger") as mock_logger:
        try:
            raise PromptValidationError("Invalid prompt")
        except AIServiceError as e:
            assert hasattr(e, "error_code")
            assert e.error_code is not None
```

**信頼性レベル**: 🔵

### テストケース 6: セキュリティ（プロンプトインジェクション対策）テスト

**目的**: ユーザー入力がプロンプトインジェクション対策されていることを確認

```python
# backend/tests/unit/test_security.py

from src.services.strands_service import StrandsAIService
from src.exceptions import InputValidationError

def test_prompt_injection_detection():
    """プロンプトインジェクション検出を確認"""
    malicious_input = "'; DROP TABLE cards; --"
    with pytest.raises(InputValidationError):
        service = StrandsAIService()
        service.validate_request({"card_content": malicious_input})

def test_user_input_sanitization():
    """ユーザー入力がサニタイズされることを確認"""
    user_answer = "<script>alert('xss')</script>"
    request = {
        "card_id": "test-001",
        "user_answer": user_answer
    }
    # サニタイズされたバージョンがプロンプトに含まれることを確認
    sanitized = service._sanitize_input(user_answer)
    assert "<script>" not in sanitized
    assert "alert" not in sanitized
```

**信頼性レベル**: 🔵

## 信頼性レベルサマリー

| 項目 | 信頼性 | 詳細 |
|------|--------|------|
| 全テスト実行 | 🔵 | REQ-SM-404/405 確定 |
| テストカバレッジ確認 | 🔵 | 80% 基準確定 |
| Protocol 準拠性確認 | 🔵 | 型安全性 mypy で検証 |
| エラーハンドリング確認 | 🔵 | 統一設計確定 |
| ログ出力確認 | 🔵 | 開発ガイドラインに準拠 |
| セキュリティ確認 | 🔵 | プロンプトインジェクション対策確認 |
| エンドポイント検証 | 🔵 | 全 3 エンドポイント動作確認 |
| ドキュメント更新 | 🔵 | CLAUDE.md, overview.md 更新 |
| 既存テスト保護 | 🔵 | 260+ テストの全通過確認 |

**総合品質**: ✅ **高品質** (100% の信頼性)

## 実装手順

### Step 1: 既存テスト実行確認（5分）

```bash
cd backend
make test 2>&1 | tee test-results.log
```

- 全テストが通過することを確認
- テスト数が 260+ であることを確認
- 実行時間を記録

### Step 2: テストカバレッジ分析（10分）

```bash
cd backend
pytest --cov=src --cov-report=term-missing --cov-report=html
open htmlcov/index.html  # macOS
# または
xdg-open htmlcov/index.html  # Linux
```

- 全体カバレッジを確認（80% 以上）
- 各モジュールのカバレッジを確認
- 80% 未満のモジュールを記録

### Step 3: 型安全性確認（10分）

```bash
cd backend
mypy src/ --strict 2>&1 | tee mypy-results.log
```

- エラー数を確認（0 件が目標）
- Protocol 実装の型チェック結果を確認

### Step 4: エンドポイント動作テスト（20分）

**4.1 ローカルサービス起動**:

```bash
cd backend
make local-all  # DynamoDB, Keycloak, Ollama
make local-api  # 別ターミナル
```

**4.2 各エンドポイントテスト**:

```bash
# /grade
curl -X POST http://localhost:8080/grade \
  -H "Content-Type: application/json" \
  -d '{"card_id": "test-001", "card_content": "test", "user_answer": "answer"}'

# /explain
curl -X POST http://localhost:8080/explain \
  -H "Content-Type: application/json" \
  -d '{"card_id": "test-002", "card_content": "test"}'

# /suggest
curl -X POST http://localhost:8080/suggest \
  -H "Content-Type: application/json" \
  -d '{"card_id": "test-003", "learning_history": ["correct"]}'
```

- 各エンドポイントが HTTP 200 で応答することを確認
- レスポンスボディが期待フォーマットであることを確認

### Step 5: USE_STRANDS フラグテスト（10分）

```bash
# USE_STRANDS=true（Ollama）
export USE_STRANDS=true
# エンドポイントテストを再実行

# USE_STRANDS=false（Bedrock）
export USE_STRANDS=false
# エンドポイントテストを再実行（AWS credentials 必要）
```

- 両モードで同じエンドポイントが動作することを確認

### Step 6: コードレビュー観点確認（15分）

**6.1 Protocol 準拠性**:

```bash
# mypy 結果をレビュー
cat mypy-results.log | head -50
```

- Protocol メソッドが全て実装されていることを確認

**6.2 エラーハンドリング**:

```bash
# エラーハンドリングパターンを確認
grep -r "except.*Error" src/ | head -20
grep -r "logger.error" src/ | head -20
```

- 統一されたエラーハンドリングパターンを確認

**6.3 ログ出力**:

```bash
grep -r "logger\." src/ | wc -l
```

- ログステートメント数を確認（150+ 件が目安）

**6.4 セキュリティ**:

```bash
# プロンプトテンプレートを確認
cat src/prompts/*.py | grep -A 5 "PROMPT ="
```

- ユーザー入力とシステムプロンプトが分離されていることを確認

### Step 7: ドキュメント更新（10分）

**7.1 CLAUDE.md 更新**:

1. `/Volumes/external/dev/memoru-liff/CLAUDE.md` を開く
2. ai-strands-migration セクションを以下に更新:
   ```markdown
   #### ai-strands-migration（AI Strands SDK 移行）
   - [x] Phase 1: 基盤構築 (TASK-0052 ~ TASK-0054)
   - [x] Phase 2: Strands SDK 統合実装 (TASK-0055 ~ TASK-0059)
   - [x] Phase 3: エラーハンドリング + アーキテクチャ強化 (TASK-0060 ~ TASK-0063)
   - [x] Phase 4: ローカル開発 + 最終検証 (TASK-0064 ~ TASK-0065)

   **最終状態**: ✅ 完了 - Bedrock から Strands Agents SDK へのマイグレーション完了
   ```

**7.2 overview.md 更新**:

1. `docs/tasks/ai-strands-migration/overview.md` を開く
2. 全タスク（TASK-0052～TASK-0065）の状態を `[x]` に更新

### Step 8: 最終コミット（5分）

```bash
cd backend

# テスト結果ログをコミット対象に追加
git add CLAUDE.md docs/tasks/ai-strands-migration/overview.md

git commit -m "TASK-0065: 全体統合テスト + 品質確認

- 全テスト通過確認（260+ テスト）
- テストカバレッジ 80% 以上確認
- Protocol 準拠性確認（mypy strict）
- エンドポイント動作検証（USE_STRANDS=true/false）
- エラーハンドリング統一性確認
- ログ出力適切性確認
- セキュリティ（プロンプトインジェクション対策）確認
- CLAUDE.md 進捗更新
- ai-strands-migration Phase 4 完了"
```

## Notes

- このタスクは Phase 1-4 の全実装のまとめであり、マイグレーション完了の最終ゲートキーパーである
- 問題が見つかった場合は該当タスク（TASK-0052～TASK-0064）に戻って修正し、その後本タスクを再実行する
- テストカバレッジ 80% は最小要件で、可能な限り 85% 以上を目指す
-既存テスト 260+ は全て保護されるべき（非回帰テスト）
- Protocol 準拠性は型安全性の根幹であり、mypy strict で検証する
- セキュリティについてはプロンプトインジェクション対策が最優先

## チェックリスト

**実装前の確認**:
- [ ] 全ての前提タスク（TASK-0063, TASK-0064）が完了していることを確認
- [ ] ローカル開発環境（DynamoDB, Keycloak, Ollama）が起動可能であることを確認
- [ ] AWS credentials がセットアップされていることを確認（Bedrock テスト用）

**実装時の確認**:
- [ ] `make test` が全テスト通過すること
- [ ] テストカバレッジが 80% 以上であること
- [ ] `mypy src/ --strict` でエラーが 0 件であること
- [ ] 全 3 エンドポイントが USE_STRANDS=true/false で動作すること
- [ ] ログに error_code が記録されていることを確認

**実装後の確認**:
- [ ] test-results.log を保存
- [ ] htmlcov/index.html でカバレッジレポート確認
- [ ] mypy-results.log を保存
- [ ] エンドポイントテストのレスポンスが期待フォーマットであることを確認
- [ ] CLAUDE.md と overview.md が更新されていることを確認
