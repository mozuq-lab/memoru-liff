# TASK-0016: カード一覧画面

**タスクID**: TASK-0016
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🟡 *user-stories.md 3.2より*

## 関連文書
- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/memoru-liff/requirements.md) - REQ-014
- **設計文書**: [architecture.md](../../design/memoru-liff/architecture.md)

## タスク概要
ユーザーが所有するフラッシュカードの一覧を表示する画面を実装する。ページネーション、次回復習日（due）の表示、カード詳細へのナビゲーションを提供する。

## 依存タスク
- **前提タスク**: TASK-0007（カード管理API）, TASK-0014（ホーム画面・ナビゲーション）
- **後続タスク**: TASK-0017（カード詳細・編集画面）

## 完了条件
- [x] カード一覧表示（ページネーション）実装
- [x] 次回復習日（due）表示機能
- [x] カード詳細へのリンク実装
- [x] 単体テスト80%以上のカバレッジ達成

---

## 実装詳細

### 1. カード一覧画面 🟡
**信頼性**: 🟡 *user-stories.md 3.2より*

カード一覧のメイン画面を実装する。

```typescript
// frontend/src/pages/CardsPage.tsx
import { useEffect, useState, useCallback } from 'react';
import { useLocation } from 'react-router-dom';
import { CardList } from '../components/CardList';
import { Loading } from '../components/common/Loading';
import { Error } from '../components/common/Error';
import { cardsApi } from '../services/api';
import { Card } from '../types/card';

const PAGE_SIZE = 20;

export const CardsPage = () => {
  const location = useLocation();
  const [cards, setCards] = useState<Card[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [isLoadingMore, setIsLoadingMore] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [hasMore, setHasMore] = useState(true);
  const [cursor, setCursor] = useState<string | null>(null);
  const [successMessage, setSuccessMessage] = useState<string | null>(
    location.state?.message || null
  );

  const fetchCards = useCallback(async (isInitial = false) => {
    if (isInitial) {
      setIsLoading(true);
      setCards([]);
      setCursor(null);
    } else {
      setIsLoadingMore(true);
    }
    setError(null);

    try {
      const response = await cardsApi.getCards({
        limit: PAGE_SIZE,
        cursor: isInitial ? undefined : cursor,
      });

      if (isInitial) {
        setCards(response.cards);
      } else {
        setCards(prev => [...prev, ...response.cards]);
      }

      setCursor(response.nextCursor);
      setHasMore(!!response.nextCursor);
    } catch (err) {
      setError('カードの取得に失敗しました');
    } finally {
      setIsLoading(false);
      setIsLoadingMore(false);
    }
  }, [cursor]);

  useEffect(() => {
    fetchCards(true);
  }, []);

  useEffect(() => {
    if (successMessage) {
      const timer = setTimeout(() => setSuccessMessage(null), 3000);
      return () => clearTimeout(timer);
    }
  }, [successMessage]);

  const handleLoadMore = () => {
    if (!isLoadingMore && hasMore) {
      fetchCards(false);
    }
  };

  if (isLoading) {
    return <Loading message="カードを読み込み中..." />;
  }

  if (error) {
    return <Error message={error} onRetry={() => fetchCards(true)} />;
  }

  return (
    <div className="pb-20">
      <header className="bg-white shadow-sm p-4 mb-4">
        <h1 className="text-xl font-bold text-gray-800">カード一覧</h1>
        <p className="text-sm text-gray-600">{cards.length}枚のカード</p>
      </header>

      {/* 成功メッセージ */}
      {successMessage && (
        <div className="mx-4 mb-4 p-3 bg-green-100 border border-green-300 text-green-700 rounded-lg">
          {successMessage}
        </div>
      )}

      <div className="px-4">
        {cards.length === 0 ? (
          /* 空状態 */
          <div className="text-center py-12">
            <svg
              className="mx-auto h-16 w-16 text-gray-400 mb-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={1}
                d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"
              />
            </svg>
            <p className="text-gray-600 mb-4">カードがありません</p>
            <a
              href="/generate"
              className="inline-block px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 min-h-[44px]"
            >
              カードを作成する
            </a>
          </div>
        ) : (
          <>
            <CardList cards={cards} />

            {/* もっと読み込むボタン / 無限スクロール */}
            {hasMore && (
              <div className="mt-6 text-center">
                <button
                  onClick={handleLoadMore}
                  disabled={isLoadingMore}
                  className="px-6 py-3 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 min-h-[44px]"
                >
                  {isLoadingMore ? '読み込み中...' : 'もっと読み込む'}
                </button>
              </div>
            )}

            {!hasMore && cards.length > 0 && (
              <p className="text-center text-gray-500 mt-6">
                すべてのカードを表示しました
              </p>
            )}
          </>
        )}
      </div>
    </div>
  );
};
```

**実装ファイル**: `frontend/src/pages/CardsPage.tsx`

### 2. カードリストコンポーネント 🟡
**信頼性**: 🟡 *user-stories.md 3.2より*

カードのリスト表示コンポーネントを実装する。

```typescript
// frontend/src/components/CardList.tsx
import { Link } from 'react-router-dom';
import { Card } from '../types/card';
import { formatDueDate, getDueStatus } from '../utils/date';

interface CardListProps {
  cards: Card[];
}

export const CardList = ({ cards }: CardListProps) => {
  return (
    <div className="space-y-3">
      {cards.map((card) => (
        <CardListItem key={card.id} card={card} />
      ))}
    </div>
  );
};

interface CardListItemProps {
  card: Card;
}

const CardListItem = ({ card }: CardListItemProps) => {
  const dueStatus = getDueStatus(card.due);

  const statusColors = {
    overdue: 'text-red-600 bg-red-50',
    today: 'text-orange-600 bg-orange-50',
    upcoming: 'text-green-600 bg-green-50',
    future: 'text-gray-600 bg-gray-50',
  };

  return (
    <Link
      to={`/cards/${card.id}`}
      className="block bg-white rounded-lg shadow p-4 hover:bg-gray-50 active:bg-gray-100"
    >
      <div className="flex justify-between items-start">
        <div className="flex-1 min-w-0">
          <p className="text-gray-800 font-medium truncate">{card.front}</p>
          <p className="text-gray-500 text-sm mt-1 truncate">{card.back}</p>
        </div>
        <div className="ml-4 flex-shrink-0">
          <span className={`text-xs px-2 py-1 rounded ${statusColors[dueStatus.status]}`}>
            {dueStatus.label}
          </span>
        </div>
      </div>
      <div className="mt-2 text-xs text-gray-400">
        次回復習: {formatDueDate(card.due)}
      </div>
    </Link>
  );
};
```

**実装ファイル**: `frontend/src/components/CardList.tsx`

### 3. 日付ユーティリティ 🔵
**信頼性**: 🔵 *architecture.mdより*

復習日に関するユーティリティ関数を実装する。

```typescript
// frontend/src/utils/date.ts
import { format, isToday, isBefore, isAfter, addDays, parseISO } from 'date-fns';
import { ja } from 'date-fns/locale';

export interface DueStatus {
  status: 'overdue' | 'today' | 'upcoming' | 'future';
  label: string;
}

export const formatDueDate = (due: string): string => {
  const date = parseISO(due);
  return format(date, 'M月d日(E)', { locale: ja });
};

export const getDueStatus = (due: string): DueStatus => {
  const dueDate = parseISO(due);
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  if (isBefore(dueDate, today)) {
    return { status: 'overdue', label: '期限切れ' };
  }

  if (isToday(dueDate)) {
    return { status: 'today', label: '今日' };
  }

  const threeDaysLater = addDays(today, 3);
  if (isBefore(dueDate, threeDaysLater)) {
    return { status: 'upcoming', label: 'もうすぐ' };
  }

  return { status: 'future', label: formatDueDate(due) };
};
```

**実装ファイル**: `frontend/src/utils/date.ts`

### 4. API連携（ページネーション対応） 🔵
**信頼性**: 🔵 *architecture.mdより*

ページネーション対応のAPI連携を実装する。

```typescript
// frontend/src/services/api.ts (部分)
interface GetCardsParams {
  limit?: number;
  cursor?: string;
}

interface GetCardsResponse {
  cards: Card[];
  nextCursor: string | null;
}

export const cardsApi = {
  // ... 他のメソッド

  getCards: async (params?: GetCardsParams): Promise<GetCardsResponse> => {
    const response = await apiClient.get<GetCardsResponse>('/cards', {
      params: {
        limit: params?.limit || 20,
        cursor: params?.cursor,
      },
    });
    return response.data;
  },
};
```

**実装ファイル**: `frontend/src/services/api.ts`

---

## 単体テスト要件

### テストケース1: カード一覧の表示 🟡
**信頼性**: 🟡 *user-stories.md 3.2より*
**Given**: カードが存在する
**When**: カード一覧画面にアクセス
**Then**: カードの一覧が表示される

### テストケース2: 次回復習日の表示 🔵
**信頼性**: 🔵 *REQ-014より*
**Given**: カードに復習日（due）が設定されている
**When**: カード一覧画面に表示される
**Then**: 復習日が日本語フォーマットで表示される

### テストケース3: 期限切れカードの表示 🟡
**信頼性**: 🟡 *要件から妥当な推測*
**Given**: 復習日が過去のカード
**When**: カード一覧画面に表示される
**Then**: 「期限切れ」ステータスが表示される

### テストケース4: 今日が復習日のカード 🟡
**信頼性**: 🟡 *要件から妥当な推測*
**Given**: 復習日が今日のカード
**When**: カード一覧画面に表示される
**Then**: 「今日」ステータスが表示される

### テストケース5: ページネーション 🔵
**信頼性**: 🔵 *architecture.mdより*
**Given**: カードが20枚以上存在する
**When**: 「もっと読み込む」ボタンをクリック
**Then**: 追加のカードが読み込まれる

### テストケース6: カード詳細への遷移 🟡
**信頼性**: 🟡 *user-stories.md 3.2より*
**Given**: カード一覧が表示されている
**When**: カードをタップ
**Then**: カード詳細画面に遷移する

### テストケース7: 空状態の表示 🟡
**信頼性**: 🟡 *要件から妥当な推測*
**Given**: カードが1枚も存在しない
**When**: カード一覧画面にアクセス
**Then**: 空状態メッセージとカード作成ボタンが表示される

### テストケース8: スケルトンローディング 🟡
**信頼性**: 🟡 *NFR-201より*
**Given**: カード読み込み中
**When**: カード一覧画面にアクセス
**Then**: スケルトンローディングが表示される

---

## UI/UX要件

### ローディング状態 🔵
**信頼性**: 🔵 *NFR-201より*
- 初期読み込み時はローディングスピナーを表示
- 追加読み込み時はボタンに「読み込み中...」を表示

### スケルトンローディング 🟡
**信頼性**: 🟡 *NFR-201より*
- カード読み込み中はスケルトン表示
- カードの形状に合わせたプレースホルダー

### エラー表示 🟡
**信頼性**: 🟡 *要件から妥当な推測*
- API取得エラー時はエラーメッセージを表示
- 再試行ボタンを配置

### 空状態 🟡
**信頼性**: 🟡 *要件から妥当な推測*
- カードがない場合は適切なメッセージを表示
- カード作成画面へのリンクを配置
- アイコンを使用して視覚的に分かりやすく

### モバイル対応 🔵
**信頼性**: 🔵 *NFR-201より*
- カードは縦に並べてリスト表示
- タップ領域は44px以上を確保
- スクロールはネイティブの動作を活用

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0016` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項
- 大量のカードがある場合のパフォーマンスを考慮する
- 無限スクロールとボタン方式の両方を検討し、ユーザビリティを優先
- カードの表示順序はAPIのデフォルト（due昇順）に従う
- date-fnsを使用して日付フォーマットを行う

---

## 信頼性レベルサマリー
- 🔵 **青信号**: 6項目
- 🟡 **黄信号**: 10項目
- 🔴 **赤信号**: 0項目

**品質評価**: 要改善（黄信号項目の詳細確認が必要）
