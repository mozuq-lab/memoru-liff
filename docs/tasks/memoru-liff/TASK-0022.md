# TASK-0022: 本番デプロイ準備

**タスクID**: TASK-0022
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 4 - 統合テスト
**信頼性レベル**: 🟡 *一般的なデプロイプラクティスから推測*

## 関連文書
- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/memoru-liff/requirements.md) - NFR-101, NFR-102
- **設計文書**: [architecture.md](../../design/memoru-liff/architecture.md)
- **受け入れ基準**: [acceptance-criteria.md](../../spec/memoru-liff/acceptance-criteria.md)

## タスク概要
本番環境へのデプロイに必要な設定・インフラ構築を行う。SAMテンプレートの本番環境設定、Secrets Manager連携、GitHub ActionsによるCI/CDパイプライン、CloudWatchによる監視・アラート設定、運用ドキュメント整備を実施する。

## 依存タスク
- **前提タスク**: TASK-0020（E2Eテスト）, TASK-0021（LINE統合テスト）
- **後続タスク**: なし（最終フェーズ）

## 完了条件
- [ ] 本番環境用SAMテンプレート作成
- [ ] Secrets Manager設定完了
- [ ] CI/CDパイプライン（GitHub Actions）構築
- [ ] 監視・アラート設定（CloudWatch）
- [ ] ドキュメント整備

---

## 実装詳細

### 1. 本番環境用SAM設定 🟡
**信頼性**: 🟡 *AWS SAMベストプラクティスから推測*

本番環境用のSAMテンプレートとsamconfigを設定する。

```toml
# backend/samconfig.toml
version = 0.1

[default.deploy.parameters]
stack_name = "memoru-liff-dev"
resolve_s3 = true
s3_prefix = "memoru-liff-dev"
region = "ap-northeast-1"
confirm_changeset = true
capabilities = "CAPABILITY_IAM CAPABILITY_AUTO_EXPAND"
parameter_overrides = "Environment=dev"

[prod.deploy.parameters]
stack_name = "memoru-liff-prod"
resolve_s3 = true
s3_prefix = "memoru-liff-prod"
region = "ap-northeast-1"
confirm_changeset = true
capabilities = "CAPABILITY_IAM CAPABILITY_AUTO_EXPAND"
parameter_overrides = "Environment=prod LineChannelSecretArn=arn:aws:secretsmanager:ap-northeast-1:ACCOUNT:secret:memoru/line-channel-secret LineChannelAccessTokenArn=arn:aws:secretsmanager:ap-northeast-1:ACCOUNT:secret:memoru/line-channel-access-token KeycloakClientSecretArn=arn:aws:secretsmanager:ap-northeast-1:ACCOUNT:secret:memoru/keycloak-client-secret"
```

```yaml
# backend/template.yaml (追加部分)
Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - staging
      - prod

  LineChannelSecretArn:
    Type: String
    Default: ""

  LineChannelAccessTokenArn:
    Type: String
    Default: ""

  KeycloakClientSecretArn:
    Type: String
    Default: ""

Conditions:
  IsProd: !Equals [!Ref Environment, prod]

Globals:
  Function:
    Timeout: 30
    MemorySize: !If [IsProd, 512, 256]
    Environment:
      Variables:
        ENVIRONMENT: !Ref Environment
        LOG_LEVEL: !If [IsProd, INFO, DEBUG]
```

**実装ファイル**: `backend/samconfig.toml`, `backend/template.yaml`

### 2. Secrets Manager設定 🟡
**信頼性**: 🟡 *AWS Secrets Managerベストプラクティスから推測*

機密情報をSecrets Managerで管理する設定を追加する。

```yaml
# backend/template.yaml (Secrets参照部分)
Resources:
  MemoruApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      # ... 既存設定 ...
      Environment:
        Variables:
          LINE_CHANNEL_SECRET: !If
            - IsProd
            - !Sub '{{resolve:secretsmanager:${LineChannelSecretArn}:SecretString:value}}'
            - !Ref AWS::NoValue
          LINE_CHANNEL_ACCESS_TOKEN: !If
            - IsProd
            - !Sub '{{resolve:secretsmanager:${LineChannelAccessTokenArn}:SecretString:value}}'
            - !Ref AWS::NoValue
      Policies:
        - !If
          - IsProd
          - Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource:
                  - !Ref LineChannelSecretArn
                  - !Ref LineChannelAccessTokenArn
                  - !Ref KeycloakClientSecretArn
          - !Ref AWS::NoValue
```

**実装ファイル**: `backend/template.yaml`

### 3. GitHub Actions CI/CDパイプライン 🟡
**信頼性**: 🟡 *GitHub Actionsベストプラクティスから推測*

デプロイ用のGitHub Actionsワークフローを構築する。

```yaml
# .github/workflows/deploy.yml
name: Deploy to AWS

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod

permissions:
  id-token: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run tests
        run: |
          cd backend
          pytest tests/ -v --cov=app --cov-report=xml

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Run frontend tests
        run: |
          cd frontend
          npm run test

      - name: Build frontend
        run: |
          cd frontend
          npm run build

  deploy-backend:
    needs: test
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ap-northeast-1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up SAM
        uses: aws-actions/setup-sam@v2

      - name: Build SAM application
        run: |
          cd backend
          sam build

      - name: Deploy to AWS
        run: |
          cd backend
          sam deploy --config-env ${{ github.event.inputs.environment || 'dev' }} --no-confirm-changeset --no-fail-on-empty-changeset

  deploy-frontend:
    needs: test
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ap-northeast-1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install and build
        run: |
          cd frontend
          npm ci
          npm run build
        env:
          VITE_API_URL: ${{ vars.API_URL }}
          VITE_LIFF_ID: ${{ vars.LIFF_ID }}

      - name: Deploy to S3
        run: |
          aws s3 sync frontend/dist s3://${{ vars.FRONTEND_BUCKET }} --delete

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation --distribution-id ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"
```

**実装ファイル**: `.github/workflows/deploy.yml`

### 4. CloudWatch監視・アラート設定 🟡
**信頼性**: 🟡 *AWS監視ベストプラクティスから推測*

CloudWatch Alarmsとダッシュボードを設定する。

```yaml
# backend/template.yaml (監視設定)
Resources:
  # Lambda エラーアラーム
  LambdaErrorAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: IsProd
    Properties:
      AlarmName: !Sub '${AWS::StackName}-lambda-errors'
      AlarmDescription: 'Lambda function error rate exceeds threshold'
      MetricName: Errors
      Namespace: AWS/Lambda
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 2
      Threshold: 5
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: FunctionName
          Value: !Ref MemoruApiFunction
      AlarmActions:
        - !Ref AlertSNSTopic

  # API Gateway 5xxエラーアラーム
  ApiGateway5xxAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: IsProd
    Properties:
      AlarmName: !Sub '${AWS::StackName}-api-5xx-errors'
      AlarmDescription: 'API Gateway 5xx error rate exceeds threshold'
      MetricName: 5XXError
      Namespace: AWS/ApiGateway
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 2
      Threshold: 10
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: ApiName
          Value: !Ref MemoruApi
      AlarmActions:
        - !Ref AlertSNSTopic

  # DynamoDB スロットリングアラーム
  DynamoDBThrottleAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: IsProd
    Properties:
      AlarmName: !Sub '${AWS::StackName}-dynamodb-throttle'
      AlarmDescription: 'DynamoDB throttling detected'
      MetricName: ThrottledRequests
      Namespace: AWS/DynamoDB
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: TableName
          Value: !Ref CardsTable
      AlarmActions:
        - !Ref AlertSNSTopic

  # SNS通知トピック
  AlertSNSTopic:
    Type: AWS::SNS::Topic
    Condition: IsProd
    Properties:
      TopicName: !Sub '${AWS::StackName}-alerts'
      DisplayName: 'Memoru LIFF Alerts'

  # CloudWatch Dashboard
  MonitoringDashboard:
    Type: AWS::CloudWatch::Dashboard
    Condition: IsProd
    Properties:
      DashboardName: !Sub '${AWS::StackName}-dashboard'
      DashboardBody: !Sub |
        {
          "widgets": [
            {
              "type": "metric",
              "x": 0, "y": 0, "width": 12, "height": 6,
              "properties": {
                "title": "Lambda Invocations & Errors",
                "metrics": [
                  ["AWS/Lambda", "Invocations", "FunctionName", "${MemoruApiFunction}"],
                  [".", "Errors", ".", "."]
                ],
                "region": "${AWS::Region}",
                "period": 300
              }
            },
            {
              "type": "metric",
              "x": 12, "y": 0, "width": 12, "height": 6,
              "properties": {
                "title": "API Gateway Latency",
                "metrics": [
                  ["AWS/ApiGateway", "Latency", "ApiName", "${MemoruApi}", {"stat": "p50"}],
                  ["...", {"stat": "p90"}],
                  ["...", {"stat": "p99"}]
                ],
                "region": "${AWS::Region}",
                "period": 300
              }
            },
            {
              "type": "metric",
              "x": 0, "y": 6, "width": 12, "height": 6,
              "properties": {
                "title": "DynamoDB Read/Write Capacity",
                "metrics": [
                  ["AWS/DynamoDB", "ConsumedReadCapacityUnits", "TableName", "${CardsTable}"],
                  [".", "ConsumedWriteCapacityUnits", ".", "."]
                ],
                "region": "${AWS::Region}",
                "period": 300
              }
            }
          ]
        }
```

**実装ファイル**: `backend/template.yaml`

### 5. 運用ドキュメント整備 🟡
**信頼性**: 🟡 *一般的なドキュメントプラクティスから推測*

READMEとデプロイ手順書を整備する。

```markdown
# README.md 更新内容

## デプロイ

### 前提条件
- AWS CLI v2がインストールされていること
- AWS SAM CLIがインストールされていること
- 適切なAWS認証情報が設定されていること

### 環境変数（Secrets Manager）
以下のシークレットをAWS Secrets Managerに作成してください：

- `memoru/line-channel-secret` - LINE Channel Secret
- `memoru/line-channel-access-token` - LINE Channel Access Token
- `memoru/keycloak-client-secret` - Keycloak Client Secret

### 手動デプロイ
```bash
# 開発環境
cd backend
sam build
sam deploy --config-env default

# 本番環境
sam deploy --config-env prod
```

### CI/CDデプロイ
GitHub Actionsで自動デプロイされます。

1. mainブランチへのプッシュで開発環境にデプロイ
2. 手動トリガーで任意の環境にデプロイ可能

### 監視
- CloudWatch Dashboard: `memoru-liff-prod-dashboard`
- アラート通知: SNS Topic `memoru-liff-prod-alerts`
```

**実装ファイル**: `README.md`

---

## テスト要件

### DIRECTタスクのため、動作確認で品質を担保

#### 確認項目1: SAMデプロイ確認
- `sam validate` でテンプレートが有効であること
- `sam build` がエラーなく完了すること

#### 確認項目2: GitHub Actions確認
- ワークフローがGitHub上で認識されること
- 必要なシークレット・変数が設定されていること

#### 確認項目3: CloudWatch確認
- アラームが正しく作成されること
- ダッシュボードにメトリクスが表示されること

---

## 実装手順

### DIRECTタスクの場合
1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項
- 本番環境へのデプロイは必ずPRレビューを経てから実行すること
- Secrets Managerのシークレットは手動で作成し、ARNをsamconfigに設定する
- CloudFront Distributionはフロントエンド用に別途作成が必要
- OIDC認証用のIAMロールをGitHub用に作成する必要がある
- 本番デプロイ前に必ずステージング環境でのテストを実施すること

---

## 信頼性レベルサマリー
- 🔵 **青信号**: 0項目
- 🟡 **黄信号**: 5項目（SAM設定、Secrets Manager、GitHub Actions、CloudWatch、ドキュメント）
- 🔴 **赤信号**: 0項目

**品質評価**: 要改善 - AWSベストプラクティスに基づく設定だが、実環境での検証が必要
