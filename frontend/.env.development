# API Base URL (local development)
VITE_API_BASE_URL=http://localhost:8080

# LIFF ID (from LINE Developers Console)
VITE_LIFF_ID=

# === OIDC Provider Configuration ===
# Keycloak の場合:
VITE_OIDC_AUTHORITY=http://localhost:8180/realms/memoru
VITE_OIDC_CLIENT_ID=liff-client

# Cognito の場合:
# VITE_OIDC_AUTHORITY=https://cognito-idp.ap-northeast-1.amazonaws.com/ap-northeast-1_XXXXXXXXX
# VITE_OIDC_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# === AI生成タイムアウト (ms) ===
# ローカルLLM(Ollama)は低速なため開発時は長め(180秒)に設定。本番は未設定で既定 30s/90s。
# backend env.json の AI_AGENT_TIMEOUT_SECONDS(秒) と整合させる(フロント >= バックエンド)。
VITE_MAX_GENERATION_TIME=180000
VITE_MAX_URL_GENERATION_TIME=180000

# === AIチューターのタイムアウト (ms) ===
# チューターのセッション開始(あいさつ生成)/メッセージ送信は AI 生成を伴うため、
# ローカルLLMでは 30 秒の既定では中断されやすい。backend のチューター用タイムアウト
# (env.json の AI_AGENT_TIMEOUT_SECONDS=180秒 を継承) 以上に設定する(190秒)。
VITE_TUTOR_AI_TIMEOUT_MS=190000
